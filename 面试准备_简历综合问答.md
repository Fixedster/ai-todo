# 简历面试问答 — 补充文档

---

## 说明

这份文档针对简历上**项目文档没覆盖到的问题**，包括：AI 辅助开发能力、第三个项目（科院访客管理系统）、个人经历与软技能、基础技术问题。

---

## 面试 Q&A

### 1. 你简历写"熟悉 AI 辅助开发"，具体怎么用的？能举个例子吗？

**回答：**
我平时开发大量借助 AI 工具（Claude Code、Cursor），基本流程是：

比如我要做一个"任务导出 Excel"的功能。我会先告诉 AI 我的需求和技术栈（Spring Boot + MyBatis-Plus），AI 会给我一个方案，包括建什么类、接口怎么设计。我会 review 这个方案，觉得没问题就让 AI 写代码。写完我会检查一遍，理解每一段在做什么，然后跑起来测试。

**用 AI 不代表不会写**——我会看生成的代码，理解逻辑，有不对的地方自己手改。等于 AI 帮我提速，但我自己得知道怎么写是对的。

**我的优势是：** 很多人只会用 ChatGPT 聊天，但我能把 AI 嵌入到完整的开发流程里——从需求分析、方案设计、代码实现到测试，全流程用 AI 提效。

### 2. 你简历上的 SDD、SKILL、MCP 是什么？解释一下。

**回答：**

这三个都是 AI 辅助开发里的概念，我说一下我的理解：

- **SDD（Schema-Driven Development）**：先定义数据结构和接口规范，再写业务逻辑。就是"先画蓝图再盖楼"，这样 AI 生成代码时上下文更清晰，生成的质量更高。

- **SKILL（结构化提示词工程）**：写提示词不是随便说一句话，而是有结构的——包含角色定义、任务目标、约束条件、输出格式等。比如我写提示词会让 AI"以 Java 后端开发专家身份，用 Spring Boot 实现一个分页查询接口，返回统一格式 Result<T>"，比直接说"帮我写个查询"效果好得多。

- **MCP（Model Context Protocol）**：可以理解为 AI 工具和外部系统之间的"翻译器"。比如 AI 可以直接通过 MCP 查数据库、读文件、操作 Git，不需要我手动把数据复制粘贴给 AI。

**说实话：** 这些概念我是在实际使用中接触到的，能说清楚是什么、怎么用，但如果追问特别深的原理，我可以说"目前还在实践中加深理解"。

### 3. 你做了三个项目，分别说说它们有什么不同？

**回答：**

| 项目 | 定位 | 规模 | 特色 |
|------|------|------|------|
| AI Todo | 个人效率工具 | 3 张表、30+ 接口 | AI 拆任务、番茄钟、数据统计 |
| Campus Forum | 社区社交平台 | 11 张表、44 个接口 | 文章互动、消息通知、后台管理、OAuth2 |
| 科院访客管理 | 企业级管理系统 | 访客申请→审批→配置 | 系统配置热更新、Excel 导出、定时任务 |

三个项目难度递进：AI Todo 功能最精炼，Campus Forum 规模最大、社交逻辑最复杂，访客管理系统偏企业实战（配置热更新、数据导出、定时清理）。

### 4. 科院访客管理系统的"配置热更新"具体怎么实现的？

**回答：**
这个功能的场景是：管理员想修改"爽约几次拉黑""入校时段"等规则，如果每次改都要重启服务就很麻烦。

我的做法：
1. 把配置以 JSON 格式存到数据库（比如 `{"max_miss_count": 3, "enter_time": "08:00-18:00"}`）
2. 系统启动时加载到 Redis 缓存
3. 管理员修改配置 → 更新数据库 → 同时更新 Redis 缓存
4. 业务代码每次从 Redis 读配置（不读数据库），所以改了立即生效，不用重启

这就是"热更新"——配置改了立马生效，服务不停机。

### 5. 你了解分布式基础概念，具体了解哪些？

**回答：**
目前是"初步了解"的阶段，主要是通过项目实践和自学：

- **缓存**：在项目里用了 Redis 做缓存，知道缓存穿透、缓存雪崩的概念，也做了相应的处理（黑名单自动过期、浏览量防刷）
- **消息队列**：知道 RabbitMQ/Kafka 的基本用途（异步解耦、削峰填谷），但在实际项目中还没用过
- **分布式锁**：了解 Redis 分布式锁的基本思路（SETNX），还没在项目里实际应用

这块是我下一步重点学习的方向，已经有计划在后续项目里引入消息队列。

### 6. 你在学校担任学生会部长和跆拳道协会会长，怎么平衡学习和开发的？

**回答：**
说实话确实挺忙的，但我有一个原则——**做一件事就专注做好**。

具体来说：
- 学生活动集中在那几个时间节点（运动会、迎新、晚会），我会提前把学习任务安排好，错开高峰期
- 项目开发利用周末和寒暑假的大块时间，平时零碎时间看文档、写小模块
- 当会长和部长锻炼了我的**沟通协调能力**，比如跟老师对接活动审批、跟干事分配任务，这些对团队协作是有帮助的

其实时间管理本身就是一种能力，做项目的时候要排优先级，跟做学生工作是一样的逻辑。

### 7. 你为什么选择 AI 工程研发这个方向？

**回答：**
两个原因：

第一，我在做项目的过程中发现，**AI 正在改变开发方式**。以前写一个功能要从零搭框架、查文档、一点一点调，现在用 AI 辅助可以大幅提效。我觉得未来每个开发者都需要具备 AI 辅助开发的能力，我想走在这个趋势前面。

第二，我对"用技术解决实际问题"很有热情。不管是做 AI Todo 提升个人效率，还是做访客系统解决校园管理问题，看到自己的代码真正被人用起来，很有成就感。

### 8. 你的技术栈里前后端都有，更偏向哪个方向？

**回答：**
目前更偏向**后端开发**。虽然前端我也能做（React、Hooks、组件通信），但我的核心兴趣在：

- 业务逻辑和数据流转（服务端 API 设计、数据库表关系）
- 系统性能和安全性（缓存策略、索引优化、鉴权方案）
- AI 能力和后端系统的集成

前端我借助 AI 能完成开发，但深度钻研我选后端。

### 9. 你在项目中遇到过什么让你印象深刻的 Bug？

**回答：**
说一个 AI Todo 项目里的。番茄钟倒计时到 0 的时候，有时会向后端发两次"结束"请求。排查发现是因为定时器自动触发完成和用户手点"完成"同时发生了，两个逻辑都没做防重复处理。

修复方法就是加了一个"正在完成中"的标记——一旦开始处理完成就锁住，不管触发几次只执行一次。这个 bug 让我养成了一个习惯：**涉及到状态切换的地方，一定要考虑并发触发的问题。**

### 10. 你对自己未来 1-2 年的规划是什么？

**回答：**

短期（接下来半年）：找到一份 AI 工程研发或 Java 后端的实习，在真实工业环境里积累经验，尤其是分布式系统和微服务方向。

中期（1-2 年）：技术上深入后端，特别是高并发、分布式、中间件这些方向。同时保持 AI 辅助开发的领先优势，探索更多 AI 和工程结合的场景。

长期来看，我的目标是成为一个**能独立搞定完整系统的工程师**——从需求分析到架构设计到上线运维，都能拿得起来。

---

## 简历上可能被追问的点

| 简历内容 | 面试官可能问什么 | 怎么答 |
|----------|-----------------|--------|
| "了解分布式基础概念" | "具体了解哪些？用到过吗？" | 诚实说目前是理论阶段，项目里用了缓存，消息队列还在学习 |
| "熟悉 SDD/SKILL/MCP" | "解释一下分别是什么" | 用大白话解释，结合开发场景（参考第 2 题） |
| 专业排名前 10% | "怎么做到的？" | 上课认真、考前系统复习、项目实践经验帮助理解理论知识 |
| 跆拳道黑带二段 | "练了多久？对你有什么影响？" | 练了很多年，培养了毅力和抗压能力，比赛经历让人学会面对失败 |
| 三个项目时间重叠 | "怎么同时做这么多项目？" | 时间错开安排，寒暑假集中攻坚，平时维护迭代 |
