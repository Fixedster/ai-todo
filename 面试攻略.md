# AI Todo 项目面试全攻略

> 本文档从面试官视角出发，系统梳理项目每个技术细节，确保你能自信地讲解每一行关键代码。

---

## 一、项目一句话介绍

这是一个融合 **AI 智能拆解、番茄钟专注、数据可视化**的全栈待办清单应用，采用前后端分离架构，前端 React 19 + Ant Design 6，后端 Spring Boot 3.2 + MyBatis-Plus，数据库 MySQL + Redis，支持 Docker 一键部署。

**面试开场话术**：

> 这个项目是我独立开发的全栈待办清单应用，核心亮点有三个：一是集成了 DeepSeek 大模型实现 AI 智能任务拆解；二是设计了 JWT 双 Token 认证体系配合 Redis 黑名单实现安全登出；三是前端实现了无感 Token 刷新机制。技术栈方面，前端用 React 19 + Zustand + Ant Design 6，后端用 Spring Boot 3.2 + MyBatis-Plus + Spring Security，数据层 MySQL 8 + Redis 7，部署用 Docker Compose 一键编排。

---

## 二、整体架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   浏览器     │────▶│   Nginx      │────▶│  Spring Boot │
│  React 19   │◀────│  反向代理     │◀────│   3.2 后端    │
│  Vite 8     │     │  静态资源     │     │             │
└─────────────┘     └──────────────┘     └──────┬──────┘
                                                 │
                                    ┌────────────┼────────────┐
                                    ▼            ▼            ▼
                              ┌──────────┐ ┌──────────┐ ┌──────────┐
                              │ MySQL 8  │ │ Redis 7  │ │ DeepSeek │
                              │ 持久化    │ │ 缓存/黑名单│ │ AI API   │
                              └──────────┘ └──────────┘ └──────────┘
```

### 架构分层说明

| 层级 | 技术 | 职责 |
|------|------|------|
| 前端展示层 | React 19 + TypeScript + Ant Design 6 | 页面渲染、用户交互、状态管理 |
| 前端构建层 | Vite 8 + Tailwind CSS 4 | 模块打包、样式处理 |
| 反向代理层 | Nginx | 静态资源服务、API 反向代理、解决跨域 |
| 后端业务层 | Spring Boot 3.2 + Spring Security | 业务逻辑、认证授权、API 接口 |
| 数据持久层 | MyBatis-Plus 3.5 + MySQL 8 | ORM 映射、数据持久化 |
| 缓存层 | Redis 7 | 统计数据缓存、Token 黑名单 |
| AI 服务层 | DeepSeek API | 任务智能拆解 |

### 请求完整链路

```
用户操作 → React 组件 → Zustand Store → Axios 请求 → Nginx 反向代理
→ Spring Security 过滤器链 → JwtAuthenticationFilter → Controller 
→ Service → MyBatis-Plus Mapper → MySQL / Redis
```

### 前端页面架构

```
App.tsx
├── ConfigProvider（Ant Design 主题配置）
├── /login → Login（登录/注册页，无需认证）
└── / → PrivateRoute（路由守卫）
    └── Layout（侧边栏 + 顶栏 + 内容区）
        ├── / → Dashboard（仪表盘：统计卡片 + 今日任务 + 优先级分布）
        ├── /tasks → Tasks（任务管理：筛选 + 列表 + 分页 + AI拆解）
        ├── /pomodoro → Pomodoro（番茄钟：计时器 + 专注记录）
        ├── /statistics → Statistics（数据统计：趋势图 + 优先级 + 标签频率）
        ├── /settings → Settings（系统设置：主题 + 导出 + 通知）
        └── /profile → Profile（个人中心：用户信息 + 编辑）
```

**面试话术**：

> 项目采用前后端分离架构。前端通过 Nginx 部署静态资源并反向代理后端 API，解决跨域问题。后端 Spring Boot 负责业务逻辑，MySQL 做持久化，Redis 做缓存和 Token 黑名单，DeepSeek API 提供 AI 任务拆解能力。部署时用 Docker Compose 编排四个服务，一键启动。

---

## 三、技术栈详解

### 3.1 前端技术栈

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| React | 19 | 最新版本，并发渲染、自动批处理，性能更优 |
| TypeScript | 6 | 类型安全，编译期发现错误，提升代码质量 |
| Vite | 8 | 基于 ESM 的开发服务器，冷启动极快，HMR 即时 |
| Ant Design | 6 | 企业级 UI 组件库，开箱即用，主题定制能力强 |
| Zustand | 5 | 轻量状态管理，API 简洁，无样板代码 |
| Recharts | 3 | 基于 React 的图表库，声明式 API，易于定制 |
| Tailwind CSS | 4 | 原子化 CSS，快速布局，减少自定义样式 |
| Axios | - | HTTP 客户端，拦截器机制支持无感刷新 |
| react-router-dom | 7 | 前端路由，支持嵌套路由和路由守卫 |
| dayjs | - | 轻量日期库，替代 moment.js，体积仅 2KB |

### 3.2 后端技术栈

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| Spring Boot | 3.2 | 快速开发框架，自动配置，生态完善 |
| Java | 17 | LTS 版本，支持 Record、Sealed Class 等新特性 |
| MyBatis-Plus | 3.5 | MyBatis 增强工具，通用 CRUD、分页插件、代码生成 |
| Spring Security | 6 | 安全框架，认证授权、过滤器链 |
| JWT (jjwt) | 0.12 | 无状态认证，适合前后端分离 |
| Redis | 7 | 内存数据库，缓存 + 黑名单，TTL 自动过期 |
| MySQL | 8 | 关系型数据库，事务支持，JSON 字段支持 |
| Docker Compose | - | 容器编排，一键部署，环境一致 |
| Lombok | - | 减少样板代码（getter/setter/constructor） |
| Jackson | - | JSON 序列化/反序列化，Spring Boot 默认集成 |

### 3.3 为什么选这些技术？（面试常问）

**Q：为什么选 React 而不是 Vue？**

> React 的函数式编程思想和 Hooks 机制更灵活，适合复杂交互场景。React 19 的并发渲染和自动批处理提升了性能。另外 React 生态更丰富，Ant Design、Recharts 等组件库成熟度高。

**Q：为什么选 Zustand 而不是 Redux？**

> 三个原因：一是代码量少，Zustand 一个 store 只需要一个 create 函数，Redux 需要 action、reducer、saga 三层；二是性能好，Zustand 基于 selector 订阅，只有用到的字段变化才触发重渲染；三是 TypeScript 支持好，类型推导更自然。对于中小型项目，Zustand 是更务实的选择。

**Q：为什么选 MyBatis-Plus 而不是 JPA？**

> MyBatis-Plus 在 MyBatis 基础上提供了通用 CRUD 和分页插件，开发效率高，同时保留了 SQL 的灵活控制权。JPA 的自动建表和懒加载在复杂场景下容易产生 N+1 查询问题，MyBatis-Plus 更可控。

**Q：为什么用 Java 17 而不是 Java 8？**

> Java 17 是 LTS 版本，长期支持。相比 Java 8，有 Record 类简化 DTO、Switch 表达式、文本块（"""多行字符串）、Sealed Class 等新特性。Spring Boot 3.x 也要求最低 Java 17。

**Q：为什么用 Vite 而不是 Webpack？**

> Vite 基于 ESM 的开发服务器，冷启动只需毫秒级（Webpack 需要打包整个应用才启动）。HMR 速度与项目规模无关，无论多大项目都是即时更新。生产构建用 Rollup，Tree-shaking 效果更好。

**Q：为什么用 Redis 而不是本地缓存（如 Caffeine）？**

> 三个原因：一是分布式场景下本地缓存无法共享，Redis 作为独立服务可以跨进程访问；二是 Redis 支持 TTL 自动过期，适合 Token 黑名单场景；三是 Redis 持久化（AOF/RDB），重启不丢数据。如果只是单机应用，Caffeine 性能更好，但本项目考虑了扩展性。

---

## 四、12 大技术亮点详解

### 🔥 亮点 1：JWT 双 Token 认证 + Redis 黑名单

**涉及文件**：
- `ai-todo-backend/src/main/java/com/aitodo/security/JwtService.java`
- `ai-todo-backend/src/main/java/com/aitodo/service/AuthService.java`
- `ai-todo-backend/src/main/java/com/aitodo/controller/AuthController.java`

#### 设计思路

传统单 Token 方案的矛盾：有效期长了不安全，短了用户体验差。双 Token 机制解决了这个问题：

| Token 类型 | 有效期 | 用途 | 存储 |
|-----------|--------|------|------|
| AccessToken | 2 小时 | 日常请求认证 | 内存（Zustand Store） |
| RefreshToken | 7 天 | 刷新 AccessToken | localStorage（持久化） |

#### 核心代码解析

**Token 生成**：

```java
private String generateToken(Long userId, String username, String type, Long expiration) {
    Date now = new Date();
    Date expiryDate = new Date(now.getTime() + expiration);
    String jti = UUID.randomUUID().toString();

    return Jwts.builder()
            .subject(String.valueOf(userId))     // 用户ID
            .claim("username", username)          // 用户名
            .claim("type", type)                  // Token类型：access/refresh
            .claim("jti", jti)                    // 唯一ID（黑名单用）
            .issuedAt(now)                        // 签发时间
            .expiration(expiryDate)               // 过期时间
            .signWith(getSigningKey())             // HMAC-SHA签名
            .compact();
}
```

**关键设计点**：
- `jti`（JWT ID）是每个 Token 的唯一标识，用于黑名单机制
- `type` 字段区分 AccessToken 和 RefreshToken，防止用 RefreshToken 访问 API
- 使用 HMAC-SHA 算法签名，密钥从配置文件读取（`@Value("${jwt.secret}")`）
- 密钥长度必须 >= 256 bits（32字节），否则 HMAC-SHA256 会抛异常

**签名密钥生成**：

```java
private SecretKey getSigningKey() {
    return Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
}
```

**Token 黑名单**：

```java
public void blacklistToken(String token) {
    Claims claims = parseToken(token);
    String jti = claims.get("jti", String.class);
    Date expiration = claims.getExpiration();
    long ttl = expiration.getTime() - System.currentTimeMillis();
    if (ttl > 0) {
        redisTemplate.opsForValue().set("token:blacklist:" + jti, "1", ttl, TimeUnit.MILLISECONDS);
    }
}

private boolean isTokenBlacklisted(String jti) {
    return Boolean.TRUE.equals(redisTemplate.hasKey("token:blacklist:" + jti));
}
```

**为什么 TTL 设为 Token 剩余有效期？**

> Token 过期后本身就无法使用了，黑名单也没必要保留。设置 TTL 为剩余有效期，Token 过期时黑名单记录也自动删除，不需要手动清理，节省 Redis 内存。这是 Redis TTL 机制的巧妙利用。

**Token 验证**：

```java
public boolean validateToken(String token) {
    Claims claims = parseToken(token);
    if (claims.getExpiration().before(new Date())) {
        return false;
    }
    String jti = claims.get("jti", String.class);
    return !isTokenBlacklisted(jti);
}
```

**登录流程**：

```java
public LoginResponse login(LoginRequest request) {
    // 1. Spring Security 认证（内部调用 DaoAuthenticationProvider + BCrypt 校验密码）
    Authentication authentication = authenticationManager.authenticate(
        new UsernamePasswordAuthenticationToken(request.getUsername(), request.getPassword())
    );
    // 2. 查询用户信息
    User user = userMapper.selectOne(new LambdaQueryWrapper<User>()
            .eq(User::getUsername, request.getUsername()));
    // 3. 生成双 Token
    String accessToken = jwtService.generateAccessToken(user.getId(), user.getUsername());
    String refreshToken = jwtService.generateRefreshToken(user.getId(), user.getUsername());
    // 4. 返回
    return LoginResponse.builder()
            .accessToken(accessToken)
            .refreshToken(refreshToken)
            .user(LoginResponse.UserInfo.builder()
                    .id(user.getId())
                    .username(user.getUsername())
                    .email(user.getEmail())
                    .avatar(user.getAvatar())
                    .build())
            .build();
}
```

**注册流程**：

```java
public void register(RegisterRequest request) {
    // 1. 检查用户名是否已存在
    // 2. BCrypt 加密密码
    // 3. 插入用户记录
    // 4. 返回（不自动登录，需要用户手动登录）
}
```

**刷新流程**：

```java
public LoginResponse refreshToken(String refreshToken) {
    // 1. 验证 RefreshToken 有效性（签名+过期+黑名单）
    if (!jwtService.validateToken(refreshToken)) {
        throw new BusinessException(401, "Refresh Token 无效或已过期");
    }
    // 2. 检查 Token 类型必须是 refresh
    String tokenType = jwtService.getTokenType(refreshToken);
    if (!"refresh".equals(tokenType)) {
        throw new BusinessException(401, "无效的 Token 类型");
    }
    // 3. 检查用户状态
    Long userId = jwtService.getUserIdFromToken(refreshToken);
    User user = userMapper.selectById(userId);
    if (user == null || user.getStatus() != 1) {
        throw new BusinessException(401, "用户不存在或已被禁用");
    }
    // 4. 生成新的双 Token
    String newAccessToken = jwtService.generateAccessToken(userId, username);
    String newRefreshToken = jwtService.generateRefreshToken(userId, username);
    // 5. 旧 RefreshToken 加入黑名单（防止重放）
    jwtService.blacklistToken(refreshToken);
    // 6. 返回新 Token
    return LoginResponse.builder()...build();
}
```

**登出流程**：

```java
// AuthController.java
@PostMapping("/logout")
public Result<Void> logout(HttpServletRequest request) {
    String token = request.getHeader("Authorization");
    if (StringUtils.hasText(token) && token.startsWith("Bearer ")) {
        jwtService.blacklistToken(token.substring(7));  // AccessToken 加入黑名单
    }
    return Result.success();
}

// AuthService.java
public void logout(String accessToken, String refreshToken) {
    jwtService.blacklistToken(accessToken);   // AccessToken 黑名单
    jwtService.blacklistToken(refreshToken);  // RefreshToken 黑名单
}
```

**面试话术**：

> 我设计了双 Token 机制。AccessToken 短期有效（2小时），RefreshToken 长期有效（7天）。用户登出时，不是简单删客户端 Token，而是把 Token 的 jti 存入 Redis 黑名单，TTL 设为 Token 剩余过期时间。这样即使 Token 被截获，登出后也无法使用。刷新 Token 时，旧 RefreshToken 也会被加入黑名单，防止重放攻击。整个设计保证了安全性和用户体验的平衡。

**面试深度追问**：

**Q：如果 Redis 宕机了，黑名单机制会怎样？**

> 黑名单失效，已登出的 Token 在有效期内仍可使用。这是可用性换安全性的权衡。生产环境可以用 Redis Sentinel 或 Cluster 保证高可用。也可以在应用层做二级缓存（如 Caffeine），短时间（如5分钟）内缓存黑名单状态，Redis 不可用时降级到本地缓存。

**Q：为什么不用 JWT 的黑名单方案而用 Redis？**

> JWT 本身是无状态的，一旦签发就无法撤回。要实现"主动失效"就必须引入有状态的存储。Redis 是最合适的选择：支持 TTL 自动过期、读写性能高（微秒级）、支持持久化。数据库也可以，但每次请求都查数据库性能太差。

**Q：AccessToken 存内存、RefreshToken 存 localStorage，为什么不都存 Cookie？**

> Cookie 有 CSRF 风险（浏览器自动携带），而且有 4KB 大小限制。AccessToken 存内存最安全（XSS 也拿不到），RefreshToken 存 localStorage 是因为页面刷新后内存清空，需要持久化。如果用 HttpOnly Cookie 存储，可以防 XSS 但仍有 CSRF 风险，需要额外防护。

---

### 🔥 亮点 2：前端无感刷新 Token

**涉及文件**：`ai-todo-frontend/src/utils/request.ts`

#### 设计思路

当 AccessToken 过期，前端自动用 RefreshToken 换取新 Token，用户完全无感知。关键难点是并发请求的处理。

#### 核心代码解析

**Axios 实例创建**：

```typescript
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
})
```

**请求拦截器——自动携带 Token**：

```typescript
request.interceptors.request.use((config: InternalAxiosRequestConfig) => {
    const token = useAuthStore.getState().token
    if (token && config.headers) {
        config.headers.Authorization = `Bearer ${token}`
    }
    return config
})
```

**为什么用 `useAuthStore.getState()` 而不是 `useAuthStore()` hook？**

> Axios 拦截器在 React 组件外部运行，不能使用 React Hooks。Zustand 的 `getState()` 方法可以在组件外部获取最新状态，这是 Zustand 相比 Redux 的一个优势——store 本身就是独立的状态容器，不依赖 React 上下文。

**响应拦截器——401 自动刷新**：

```typescript
let isRefreshing = false
let refreshSubscribers: Array<(token: string) => void> = []

function onTokenRefreshed(token: string) {
    refreshSubscribers.forEach((callback) => callback(token))
    refreshSubscribers = []
}

function addRefreshSubscriber(callback: (token: string) => void) {
    refreshSubscribers.push(callback)
}

request.interceptors.response.use(
    (response) => response,
    async (error: AxiosError) => {
        const originalRequest = error.config as InternalAxiosRequestConfig & { _retry?: boolean }

        if (error.response?.status === 401 && !originalRequest._retry) {
            if (isRefreshing) {
                return new Promise((resolve) => {
                    addRefreshSubscriber((token: string) => {
                        originalRequest.headers!.Authorization = `Bearer ${token}`
                        resolve(request(originalRequest))
                    })
                })
            }

            originalRequest._retry = true
            isRefreshing = true

            try {
                const refreshToken = useAuthStore.getState().refreshToken
                const response = await axios.post(
                    `${request.defaults.baseURL}/auth/refresh`,
                    {},
                    { headers: { Authorization: `Bearer ${refreshToken}` } }
                )

                const { accessToken, refreshToken: newRefreshToken } = response.data.data
                useAuthStore.getState().setToken(accessToken, newRefreshToken)

                onTokenRefreshed(accessToken)
                isRefreshing = false

                originalRequest.headers!.Authorization = `Bearer ${accessToken}`
                return request(originalRequest)
            } catch (refreshError) {
                isRefreshing = false
                useAuthStore.getState().logout()
                message.error('登录已过期，请重新登录')
                window.location.href = '/login'
                return Promise.reject(refreshError)
            }
        }

        if (error.response?.data && (error.response.data as any).message) {
            message.error((error.response.data as any).message)
        }

        return Promise.reject(error)
    }
)
```

**并发场景图解**：

```
请求A ──▶ 401 ──▶ 发起刷新 ──▶ 拿到新Token ──▶ 重发A ✓
请求B ──▶ 401 ──▶ 排队等待 ──────────────────▶ 重发B ✓
请求C ──▶ 401 ──▶ 排队等待 ──────────────────▶ 重发C ✓
```

**关键设计点**：
- `isRefreshing` 标志位保证只发一次刷新请求
- `refreshSubscribers` 队列缓存等待中的请求回调
- `_retry` 标记防止无限重试（刷新后仍然 401 的情况）
- 刷新失败时清空队列、登出、跳转登录页
- 刷新请求用原生 `axios` 而不是 `request` 实例，避免拦截器死循环

**面试话术**：

> 前端通过 Axios 拦截器实现了无感刷新。当 AccessToken 过期返回 401 时，自动用 RefreshToken 换取新 Token。关键点是并发请求的处理：如果多个请求同时 401，我用一个队列机制保证只发一次刷新请求，其他请求排队等待，刷新成功后统一用新 Token 重发，避免重复刷新导致的 Token 竞争问题。

**面试深度追问**：

**Q：如果刷新 Token 的请求本身也 401 了怎么办？**

> `_retry` 标记防止无限循环。刷新失败后清空等待队列，执行登出逻辑，跳转登录页。不会出现无限重试的情况。

**Q：为什么刷新请求用原生 axios 而不是 request 实例？**

> 如果用 request 实例，刷新请求失败也会触发响应拦截器，再次尝试刷新，形成死循环。用原生 axios 绕过拦截器，直接发送刷新请求。

**Q：refreshSubscribers 队列会不会内存泄漏？**

> 不会。刷新成功后 `onTokenRefreshed` 会遍历所有回调并清空数组。刷新失败时也会登出跳转，不会留下悬挂的 Promise。

---

### 🔥 亮点 3：AI 智能任务拆解 + 降级策略

**涉及文件**：
- `ai-todo-backend/src/main/java/com/aitodo/service/AIService.java`
- `ai-todo-backend/src/main/java/com/aitodo/controller/AIController.java`
- `ai-todo-frontend/src/components/ai/AIDecomposeModal.tsx`

#### 设计思路

调用 DeepSeek 大模型 API，通过精心设计的 System Prompt 约定返回 JSON 数组格式。API 不可用时，根据关键词匹配预设模板降级返回。

#### 核心代码解析

**System Prompt 设计**：

```java
private static final String SYSTEM_PROMPT = """
    你是一个任务分解助手。用户会给你一个任务，你需要将其拆解为 3~6 个可执行的子任务。
    请严格以 JSON 数组格式返回，每个子任务包含以下字段：
    - title: 子任务标题（简短）
    - description: 子任务描述（一句话说明怎么做）
    - priority: 优先级（0=低, 1=中, 2=高）
    
    只返回 JSON 数组，不要返回其他任何文字、解释或 markdown 格式。
    示例格式：
    [{"title":"子任务1","description":"具体做法","priority":2},{"title":"子任务2","description":"具体做法","priority":1}]
    """;
```

**Prompt 设计要点**：
- 明确角色定位（任务分解助手）
- 约定输出格式（JSON 数组）
- 约定字段含义（title/description/priority）
- 给出示例格式（few-shot prompting）
- 强调"只返回 JSON"（减少无关输出）
- 约定子任务数量范围（3~6个）

**API 调用——Java 11+ HttpClient**：

```java
private AIDecomposeResponse callDeepSeek(String task) throws Exception {
    String requestBody = objectMapper.writeValueAsString(new LinkedHashMap<>() {{
        put("model", model);     // deepseek-chat
        put("messages", List.of(
            Map.of("role", "system", "content", SYSTEM_PROMPT),
            Map.of("role", "user", "content", "请帮我拆解这个任务：" + task)
        ));
        put("temperature", 0.7);   // 适度创造性
        put("max_tokens", 1024);   // 限制输出长度
    }});

    HttpClient client = HttpClient.newHttpClient();
    HttpRequest httpRequest = HttpRequest.newBuilder()
            .uri(URI.create(apiUrl))
            .header("Content-Type", "application/json")
            .header("Authorization", "Bearer " + apiKey)
            .POST(HttpRequest.BodyPublishers.ofString(requestBody))
            .build();

    HttpResponse<String> response = client.send(httpRequest, HttpResponse.BodyHandlers.ofString());

    if (response.statusCode() != 200) {
        throw new RuntimeException("DeepSeek API error: " + response.statusCode());
    }
}
```

**为什么用 Java 11+ HttpClient 而不是 RestTemplate？**

> HttpClient 是 Java 11 引入的原生 HTTP 客户端，支持 HTTP/2、异步请求。相比 RestTemplate（已在 Spring 5.3 标记为维护模式），HttpClient 不依赖 Spring 框架，更轻量。也可以用 WebClient，但对于简单的同步调用，HttpClient 足够了。

**AI 返回内容解析——兼容 Markdown 包裹**：

```java
String content = root.path("choices").path(0).path("message").path("content").asText();
String jsonStr = content.trim();
if (jsonStr.startsWith("```")) {
    jsonStr = jsonStr.replaceAll("```json\\s*", "").replaceAll("```\\s*", "");
}
jsonStr = jsonStr.trim();
```

**为什么需要兼容 Markdown 包裹？**

> 大模型有时会忽略"只返回 JSON"的指令，返回 ````json ... ```` 格式。这是 LLM 的常见问题，解析时必须做兼容处理。

**降级策略——三层降级**：

```java
public AIDecomposeResponse decomposeTask(AIDecomposeRequest request) {
    try {
        return callDeepSeek(request.getTask());       // 第一层：API 调用
    } catch (Exception e) {
        return getFallbackSuggestions(request.getTask()); // 第二层：关键词匹配
    }
}

private AIDecomposeResponse getFallbackSuggestions(String task) {
    String lowerTask = task.toLowerCase();
    if (lowerTask.contains("面试") || lowerTask.contains("interview")) {
        // 面试模板：准备简历、复习基础、刷算法、模拟面试
    } else if (lowerTask.contains("学习") || lowerTask.contains("study")) {
        // 学习模板：搜集资料、制定计划、动手实践、总结复盘
    } else if (lowerTask.contains("旅行") || lowerTask.contains("travel")) {
        // 旅行模板：确定行程、预订交通住宿、准备行李
    } else {
        // 第三层：通用模板兜底（明确目标、拆解步骤、分配资源、执行检查）
    }
    
    return AIDecomposeResponse.builder()
            .originalTask(task)
            .suggestions(suggestions)
            .fallback(true)  // 前端可据此显示提示
            .build();
}
```

**前端 AI 拆解交互流程**：

```typescript
// AIDecomposeModal.tsx
// 1. 用户输入任务描述
// 2. 调用 /ai/decompose 接口
// 3. 展示 AI 建议的子任务列表（可点击选择）
// 4. 用户选择要创建的子任务
// 5. 调用 /tasks/batch-create 批量创建
const handleDecompose = async () => {
    const response = await request.post('/ai/decompose', { task: taskInput })
    setSuggestions(response.data.data.suggestions || [])
}

const handleCreateTasks = async () => {
    const selectedTasks = selectedSuggestions.map((index) => suggestions[index])
    await request.post('/tasks/batch-create', selectedTasks)
    message.success(`成功创建 ${selectedTasks.length} 个子任务`)
}
```

**面试话术**：

> AI 拆解功能调用了 DeepSeek 的大模型 API。我通过 System Prompt 约定返回 JSON 数组格式。解析时兼容了 AI 可能返回 Markdown 代码块的情况。更重要的是，我设计了三层降级策略：API 调用失败时，根据任务关键词匹配预设模板返回建议，关键词也不匹配时返回通用模板，保证功能在任何情况下都可用。前端用模态框展示 AI 建议，用户可以选择性采纳，批量创建子任务。

**面试深度追问**：

**Q：如果 AI 返回的 JSON 格式不合法怎么办？**

> 解析时用 Jackson 的 `path()` 方法而不是 `get()`，`path()` 遇到不存在的字段返回 MissingNode 而不是抛异常，配合 `asText()` 和 `asInt(1)` 的默认值机制，保证不会因为格式问题崩溃。如果整个 JSON 解析失败，会被 catch 住走降级逻辑。

**Q：temperature 设为 0.7 是什么意思？**

> temperature 控制输出的随机性。0 表示确定性输出（每次相同输入得到相同输出），1 表示最大随机性。0.7 是一个折中值，既保证一定的创造性（拆解结果多样化），又不至于太离谱。对于任务拆解场景，不需要太高的创造性。

**Q：为什么不用 OpenAI 的 GPT？**

> DeepSeek 的 API 兼容 OpenAI 格式，但价格更低。对于任务拆解这种场景，DeepSeek 的能力足够。如果需要更强的推理能力，可以切换到 GPT-4，只需修改配置中的 model 和 apiUrl。

---

### 🔥 亮点 4：Redis 缓存 + 主动失效

**涉及文件**：`ai-todo-backend/src/main/java/com/aitodo/service/StatisticsService.java`

#### 设计思路

统计数据计算量大（需要聚合多张表），但实时性要求高。采用 Cache-Aside 模式：读时回源回填，写时主动删缓存。

#### 核心代码解析

**缓存读取**：

```java
@SneakyThrows
public StatisticsResponse getOverview() {
    Long userId = getCurrentUserId();
    String cacheKey = "statistics:overview:" + userId;

    String cached = redisTemplate.opsForValue().get(cacheKey);
    if (cached != null) {
        return objectMapper.readValue(cached, StatisticsResponse.class);
    }

    StatisticsResponse response = buildStatistics(userId);
    redisTemplate.opsForValue().set(cacheKey, 
        objectMapper.writeValueAsString(response), 5, TimeUnit.MINUTES);
    return response;
}
```

**主动失效**：

```java
public void invalidateCache() {
    Long userId = getCurrentUserId();
    redisTemplate.delete("statistics:overview:" + userId);
}
```

**在任务增删改中调用**：

```java
public Task createTask(TaskRequest request) {
    taskMapper.insert(task);
    statisticsService.invalidateCache();  // 主动删缓存
    return task;
}
```

**Cache-Aside 模式流程图**：

```
读操作：
  查Redis ──▶ 命中 ──▶ 返回
     │
     ▼ 未命中
  计算数据 ──▶ 写Redis(TTL 5min) ──▶ 返回

写操作：
  更新数据库 ──▶ 删除Redis缓存 ──▶ 返回
```

**统计数据构建详情**：

```java
private StatisticsResponse buildStatistics(Long userId) {
    // 1. 查询所有未删除任务
    List<Task> allTasks = taskMapper.selectList(taskWrapper);
    
    // 2. 基础统计：总数、已完成、待办
    long totalTasks = allTasks.size();
    long completedTasks = allTasks.stream().filter(t -> t.getStatus() == 2).count();
    long pendingTasks = allTasks.stream().filter(t -> t.getStatus() == 0 || t.getStatus() == 1).count();
    
    // 3. 本周趋势：按天统计完成和新建数量
    for (int i = 0; i < 7; i++) {
        LocalDate date = weekStart.plusDays(i);
        long completed = allTasks.stream()
            .filter(t -> t.getStatus() == 2 && t.getUpdatedAt().toLocalDate().equals(date))
            .count();
        long created = allTasks.stream()
            .filter(t -> t.getCreatedAt().toLocalDate().equals(date))
            .count();
    }
    
    // 4. 优先级分布：按优先级分组计数
    // 5. 标签频率：遍历所有任务的 tags JSON 字段，统计每个标签出现次数
    // 6. 番茄钟统计：今日专注时长、本周专注时长、总专注次数
}
```

**面试话术**：

> 统计数据用了 Redis 缓存 + 主动失效策略。采用 Cache-Aside 模式，读时先查缓存，未命中则计算并回填，TTL 5 分钟兜底。写操作时主动删除缓存，保证实时性。统计数据包含任务总数、完成率、本周趋势、优先级分布、标签频率、番茄钟时长等，计算量较大，缓存后接口响应从几百毫秒降到几毫秒。

**面试深度追问**：

**Q：Cache-Aside 模式有没有并发问题？**

> 有。极端情况下：线程A读缓存未命中→查数据库→线程B更新数据库→线程B删缓存→线程A写缓存，导致缓存是旧数据。但本项目是单用户场景，不会出现并发写同一用户的缓存。如果要严格保证，可以用分布式锁或双删策略（删缓存→更新数据库→延迟再删一次）。

**Q：为什么用 StringRedisTemplate 而不是 RedisTemplate？**

> StringRedisTemplate 存储的是字符串，序列化/反序列化由应用层控制（用 Jackson）。RedisTemplate 默认用 JDK 序列化，存储内容不可读，且存在反序列化安全风险。手动用 Jackson 序列化更灵活，也方便调试。

**Q：缓存击穿、穿透、雪崩了解吗？本项目怎么防？**

> - **缓存击穿**：热点 key 过期瞬间大量请求打到数据库。本项目单用户场景概率极低。
> - **缓存穿透**：查询不存在的数据。本项目不会出现，因为统计数据一定存在。
> - **缓存雪崩**：大量 key 同时过期。本项目 TTL 5 分钟是兜底，主动失效是主要机制。

---

### 🔥 亮点 5：Spring Security 过滤器链设计

**涉及文件**：
- `ai-todo-backend/src/main/java/com/aitodo/config/SecurityConfig.java`
- `ai-todo-backend/src/main/java/com/aitodo/security/JwtAuthenticationFilter.java`

#### Security 过滤器链执行顺序

```
HTTP 请求
  │
  ▼
CorsFilter（跨域处理）
  │
  ▼
Spring Security Filter Chain
  │
  ├── CsrfFilter ──▶ 已禁用（前后端分离不需要 CSRF）
  │
  ├── SessionManagementFilter ──▶ 无状态（STATELESS）
  │
  ├── JwtAuthenticationFilter ──▶ 自定义 JWT 认证过滤器
  │     1. 从 Header 提取 Bearer Token
  │     2. 验证签名、过期、黑名单
  │     3. 检查 type == "access"
  │     4. 加载 UserDetails，设置 SecurityContext
  │
  ├── AuthorizationFilter ──▶ 路径权限校验
  │     /api/auth/** → permitAll
  │     /api/** → authenticated
  │
  └── ExceptionTranslationFilter ──▶ 异常处理
        AuthenticationException → 401
        AccessDeniedException → 403
```

#### JwtAuthenticationFilter 核心逻辑

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                     HttpServletResponse response, 
                                     FilterChain filterChain) {
        try {
            String jwt = getJwtFromRequest(request);
            if (StringUtils.hasText(jwt) && jwtService.validateToken(jwt)) {
                String username = jwtService.getUsernameFromToken(jwt);
                String type = jwtService.getTokenType(jwt);

                if ("access".equals(type)) {
                    UserDetails userDetails = userDetailsService.loadUserByUsername(username);
                    UsernamePasswordAuthenticationToken authentication =
                            new UsernamePasswordAuthenticationToken(
                                userDetails, null, userDetails.getAuthorities());
                    authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                    SecurityContextHolder.getContext().setAuthentication(authentication);
                }
            }
        } catch (Exception ex) {
            logger.error("Could not set user authentication", ex);
        }
        filterChain.doFilter(request, response);
    }
}
```

**为什么继承 OncePerRequestFilter？**

> 确保每个请求只经过一次 JWT 过滤。请求转发场景下过滤器可能被多次调用，OncePerRequestFilter 保证只执行一次。

**为什么 catch 异常后还继续 filterChain.doFilter？**

> JWT 验证失败时不直接返回 401，而是让请求继续走过滤器链。后续的 AuthorizationFilter 会发现 SecurityContext 中没有认证信息，返回 401。这样职责分离，JwtAuthenticationFilter 只负责认证，授权由 Spring Security 框架处理。

**面试话术**：

> 安全方面，我配置了 Spring Security 过滤器链。禁用 CSRF，会话策略设为无状态。自定义 JwtAuthenticationFilter 在表单登录过滤器之前执行，验证 Token 后设置 SecurityContext。特别注意检查了 Token 类型，只允许 AccessToken 访问 API。异常不直接返回，而是交给后续的授权过滤器处理，实现职责分离。

---

### 🔥 亮点 6：软删除 + 定时清理

**涉及文件**：
- `ai-todo-backend/src/main/java/com/aitodo/entity/Task.java`
- `ai-todo-backend/src/main/java/com/aitodo/service/TaskService.java`
- `ai-todo-backend/src/main/java/com/aitodo/schedule/TaskCleanupJob.java`

#### 软删除设计

**MyBatis-Plus 全局配置**：

```yaml
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

**Entity 中 JSON 字段处理**：

```java
@TableField(typeHandler = JacksonTypeHandler.class)
private List<String> tags;  // MySQL JSON ↔ Java List<String>
```

**软删除操作**：

```java
public void deleteTask(Long id) {
    UpdateWrapper<Task> uw = new UpdateWrapper<>();
    uw.eq("id", id).set("is_deleted", 1).set("updated_at", LocalDateTime.now());
    taskMapper.update(null, uw);
    statisticsService.invalidateCache();
}
```

#### 定时清理——30 天后物理删除

```java
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨 2 点
public void cleanupDeletedTasks() {
    LocalDateTime thirtyDaysAgo = LocalDateTime.now().minusDays(30);
    LambdaQueryWrapper<Task> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Task::getIsDeleted, 1).lt(Task::getUpdatedAt, thirtyDaysAgo);
    List<Task> tasksToDelete = taskMapper.selectList(wrapper);
    for (Task task : tasksToDelete) {
        taskMapper.deleteById(task.getId());  // 物理删除
    }
}
```

**两阶段删除的好处**：软删除可恢复 + 定时物理删除节省空间

**面试话术**：

> 任务删除采用两阶段策略。用户删除时标记 is_deleted = 1（软删除），可恢复。定时任务每天凌晨 2 点清理软删除超过 30 天的记录做物理删除，避免数据无限膨胀。

---

### 🔥 亮点 7：批量操作 + 权限校验

**涉及文件**：`ai-todo-backend/src/main/java/com/aitodo/service/TaskService.java`

```java
public void batchOperate(BatchTaskRequest request) {
    Long userId = getCurrentUserId();
    List<Task> tasks = taskMapper.selectBatchIds(request.getIds());
    
    for (Task task : tasks) {
        if (!task.getUserId().equals(userId)) {
            throw new BusinessException("无权操作部分任务");
        }
    }

    switch (request.getAction()) {
        case "complete":      // 批量完成
        case "delete":        // 批量删除（软删除）
        case "updatePriority": // 批量修改优先级
    }
    statisticsService.invalidateCache();
}
```

**权限校验为什么在 Service 层？**

> 安全校验应靠近数据层。如果只在 Controller 校验，其他 Service 调用时可能绕过。而且批量操作涉及多条数据，必须逐条校验，防止越权操作他人任务。

**面试话术**：

> 批量操作支持完成、删除、修改优先级三种操作，以及 AI 拆解后的批量创建。关键设计是操作前逐一校验任务是否属于当前用户，防止越权。

---

### 🔥 亮点 8：全局异常处理

**涉及文件**：
- `ai-todo-backend/src/main/java/com/aitodo/common/exception/GlobalExceptionHandler.java`
- `ai-todo-backend/src/main/java/com/aitodo/common/exception/BusinessException.java`
- `ai-todo-backend/src/main/java/com/aitodo/common/Result.java`

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)      // 业务异常
    @ExceptionHandler(BadCredentialsException.class) // 认证失败 → 401
    @ExceptionHandler(AccessDeniedException.class)   // 权限不足 → 403
    @ExceptionHandler(BindException.class)           // 参数校验 → 400
    @ExceptionHandler(Exception.class)               // 兜底 → 500
}
```

**为什么兜底返回"系统错误"？**

> 防止泄露堆栈信息、SQL 语句等内部信息，避免被攻击者利用。

**BusinessException 为什么继承 RuntimeException？**

> Spring 事务默认只回滚非受检异常（RuntimeException 及其子类）。如果继承 Exception，事务不会自动回滚。

**面试话术**：

> 统一异常处理：自定义 BusinessException 携带错误码，GlobalExceptionHandler 按类型分别处理。业务异常返回错误码和消息，认证失败 401，权限不足 403，参数校验 400，未知异常返回通用提示并记录日志。

---

### 🔥 亮点 9：前端状态管理 + 路由守卫

**涉及文件**：
- `ai-todo-frontend/src/store/authStore.ts`
- `ai-todo-frontend/src/store/taskStore.ts`
- `ai-todo-frontend/src/App.tsx`

#### Auth Store——persist 中间件持久化

```typescript
export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      token: null, refreshToken: null, user: null,
      setToken: (token, refreshToken) => set({ token, refreshToken }),
      setUser: (user) => set({ user }),
      logout: () => set({ token: null, refreshToken: null, user: null }),
    }),
    { name: 'auth-storage', partialize: (state) => ({ 
        token: state.token, refreshToken: state.refreshToken, user: state.user 
    })}
  )
)
```

#### Task Store——不持久化，每次从后端获取

```typescript
export const useTaskStore = create<TaskState>((set) => ({
  tasks: [],
  loading: false,
  filters: { status: undefined, priority: undefined, keyword: undefined, tag: undefined },
  setTasks: (tasks) => set({ tasks }),
  addTask: (task) => set((state) => ({ tasks: [task, ...state.tasks] })),
  updateTask: (id, task) => set((state) => ({
    tasks: state.tasks.map((t) => (t.id === id ? { ...t, ...task } : t)),
  })),
  deleteTask: (id) => set((state) => ({
    tasks: state.tasks.filter((t) => t.id !== id),
  })),
}))
```

#### 路由守卫

```typescript
function PrivateRoute({ children }: { children: React.ReactNode }) {
  const { token } = useAuthStore()
  return token ? <>{children}</> : <Navigate to="/login" replace />
}
```

**面试话术**：

> 前端用 Zustand 管理状态。认证状态用 persist 持久化到 localStorage，任务状态不持久化。路由守卫用 PrivateRoute，未登录跳转登录页。Layout 嵌套路由，子页面通过 Outlet 渲染。

---

### 🔥 亮点 10：动态主题系统

**涉及文件**：`ai-todo-frontend/src/App.tsx`、`ai-todo-frontend/src/pages/Settings.tsx`

```typescript
<ConfigProvider theme={{
  algorithm: currentTheme === 'dark' ? theme.darkAlgorithm : theme.defaultAlgorithm,
  token: {
    colorPrimary: '#5b8a72',
    borderRadius: 10,
    colorBgLayout: '#f8f5f0',
  },
  components: {
    Menu: { itemSelectedBg: '#e4efe9' },
    Card: { borderRadius: 14 },
    Button: { primaryShadow: '0 4px 12px rgba(91,138,114,0.3)' },
  },
}}>
```

**10 种预设主题色**：森林绿、苔藓绿、暖沙、赤陶、天青、琥珀、浆果、深林、薰衣草、橄榄

**主题切换通过 CustomEvent 通信**：Settings 页面 dispatch 事件，App 组件监听更新。

---

### 🔥 亮点 11：数据导出功能

**涉及文件**：`ai-todo-frontend/src/pages/Settings.tsx`

#### CSV 导出关键细节

- `\ufeff` BOM 头：解决 Excel 中文乱码
- 双引号转义：CSV 规范 `"` → `""`
- 标签分号分隔：避免与逗号冲突
- `URL.revokeObjectURL`：释放 Blob URL，避免内存泄漏

---

### 🔥 亮点 12：Docker Compose 一键部署

**涉及文件**：`docker-compose.yml`、`Dockerfile`、`nginx.conf`

#### 关键设计

1. **healthcheck**：MySQL 用 `mysqladmin ping` 检测是否真正可用
2. **depends_on condition**：后端等 MySQL 健康检查通过才启动
3. **数据卷**：命名卷持久化数据，容器重启不丢失
4. **多阶段构建**：构建阶段（Maven/Node）→ 运行阶段（JRE/Nginx），减小镜像体积
5. **Nginx 配置**：SPA 路由回退 + 反向代理 + 静态资源缓存 + gzip

```nginx
location / {
    try_files $uri $uri/ /index.html;    # SPA 路由回退
}
location /api {
    proxy_pass http://backend:8080;       # Docker 服务名
}
location ~* \.(js|css|png|jpg)$ {
    expires 1y;                            # 静态资源缓存1年
}
```

**面试话术**：

> 部署用 Docker Compose 编排四个服务。MySQL healthcheck 保证后端等数据库就绪才启动。Dockerfile 多阶段构建减小镜像体积。Nginx 既服务静态资源又反向代理 API。

---

## 五、数据库设计

### 5.1 ER 关系图

```
┌──────────────┐         ┌──────────────────┐         ┌──────────────────┐
│   sys_user   │         │    todo_task      │         │  pomodoro_record │
├──────────────┤         ├──────────────────┤         ├──────────────────┤
│ id (PK)      │◀────────│ user_id (FK)     │         │ id (PK)          │
│ username     │         │ id (PK)          │◀────────│ task_id (FK)     │
│ password     │         │ title            │         │ user_id (FK)     │
│ email        │         │ description      │         │ duration         │
│ avatar       │         │ priority         │         │ actual_duration  │
│ status       │         │ status           │         │ status           │
│ created_at   │         │ due_date         │         │ start_time       │
│ updated_at   │         │ parent_id (自关联)│         │ end_time         │
└──────────────┘         │ tags (JSON)      │         │ created_at       │
                         │ sort_order       │         └──────────────────┘
                         │ is_deleted       │
                         │ created_at       │
                         │ updated_at       │
                         └──────────────────┘
```

### 5.2 核心表结构

#### sys_user 用户表

```sql
CREATE TABLE IF NOT EXISTS sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(20) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    email VARCHAR(50),
    avatar VARCHAR(200),
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**字段设计要点**：
- `username VARCHAR(20) UNIQUE`：UNIQUE 约束既是业务约束也是索引，注册时判断用户名是否存在走索引
- `password VARCHAR(100)`：BCrypt 加密后固定 60 字符，设 100 预留空间
- `status TINYINT`：刷新 Token 时检查用户状态，被禁用的用户即使 Token 有效也不能访问
- `utf8mb4`：支持 emoji 和特殊字符，utf8 在 MySQL 中只支持 3 字节 UTF-8，mb4 才是真正的 UTF-8

#### todo_task 任务表

```sql
CREATE TABLE IF NOT EXISTS todo_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    title VARCHAR(100) NOT NULL,
    description TEXT,
    priority TINYINT DEFAULT 1,
    status TINYINT DEFAULT 0,
    due_date DATE,
    parent_id BIGINT DEFAULT NULL,
    tags JSON,
    sort_order INT DEFAULT 0,
    is_deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_status (user_id, status, is_deleted),
    INDEX idx_due_date (due_date),
    INDEX idx_priority (priority)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**索引设计详解**：

| 索引 | 类型 | 用途 |
|------|------|------|
| idx_user_status | 复合索引 (user_id, status, is_deleted) | 任务列表最常用查询，覆盖索引 |
| idx_due_date | 单列索引 | 按截止日期筛选排序 |
| idx_priority | 单列索引 | 按优先级筛选 |

**为什么 idx_user_status 是复合索引？**

> 任务列表查询几乎总是 `WHERE user_id = ? AND status = ? AND is_deleted = 0`，复合索引覆盖三个条件，避免回表。最左前缀原则：user_id 放最前，因为每个查询都带 user_id。

**tags 为什么用 JSON 而不是关联表？**

> 标签是任务附属属性，不需要独立管理。JSON 存储简单，查询用 LIKE。如果标签需要统计、去重，应该用关联表（task_tag 中间表）。本项目标签频率统计是在应用层做的（遍历所有任务的 tags 字段），数据量小时性能足够。

**parent_id 自关联的设计**：

> 支持 AI 拆解后的子任务关联。parent_id 指向父任务的 id，形成树形结构。查询时可以先查父任务，再根据 parent_id 查子任务。如果层级深，可以用递归 CTE（MySQL 8 支持）。

#### pomodoro_record 番茄钟记录表

```sql
CREATE TABLE IF NOT EXISTS pomodoro_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    task_id BIGINT,
    duration INT NOT NULL,
    actual_duration INT,
    status TINYINT DEFAULT 0,
    start_time DATETIME NOT NULL,
    end_time DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time (user_id, start_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**为什么 duration 和 actual_duration 分开？**

> duration 是用户设定的时长（如 25 分钟），actual_duration 是实际专注时长。用户可能中途打断，actual_duration < duration。分开存储可以统计专注完成率和中断率。

### 5.3 数据库设计面试 Q&A

**Q：为什么用 BIGINT 而不是 INT 做主键？**

> BIGINT 是 8 字节，最大值 2^63-1（约 922 亿亿）。INT 是 4 字节，最大值约 21 亿。对于可能长期增长的业务，BIGINT 更安全，避免主键溢出。

**Q：为什么用 AUTO_INCREMENT 而不是 UUID？**

> 自增主键有序，B+ 树插入时追加到末尾，写入性能好。UUID 无序，插入时可能触发页分裂，性能差。而且 BIGINT 只占 8 字节，UUID 占 36 字节，索引更大。

**Q：ON UPDATE CURRENT_TIMESTAMP 的作用？**

> 记录更新时自动更新 updated_at 字段，不需要应用层手动设置。软删除时也依赖这个字段判断删除时间。

**Q：为什么用 InnoDB 而不是 MyISAM？**

> InnoDB 支持事务（ACID）、行级锁、外键约束、MVCC。MyISAM 只支持表级锁，并发写入性能差，且不支持事务和崩溃恢复。MySQL 5.5 之后默认引擎就是 InnoDB。

---

## 六、前端页面功能详解

### 6.1 登录/注册页（Login.tsx）

**功能**：登录和注册两个 Tab 切换

**登录流程**：
```
用户输入 → 表单校验（用户名3-20字符、密码6+字符）
→ POST /auth/login → 获取 accessToken + refreshToken + user
→ setToken + setUser → navigate('/') 跳转首页
```

**注册流程**：
```
用户输入 → 表单校验 + 确认密码一致性
→ POST /auth/register → 注册成功 → 切换到登录 Tab
```

**关键设计**：
- 登录和注册共用一个页面，通过 Tabs 组件切换
- 注册成功不自动登录，需要用户手动登录（安全考虑）
- 表单校验用 Ant Design Form 的 rules 机制

### 6.2 仪表盘（Dashboard.tsx）

**功能**：统计卡片 + 今日任务 + 优先级分布

**数据获取**：
```typescript
const [tasksRes, pomodoroRes] = await Promise.all([
    request.get('/tasks?page=1&size=500'),
    request.get('/pomodoro/history'),
])
```

**为什么用 Promise.all？**

> 任务列表和番茄钟记录是两个独立请求，并行获取节省时间。串行需要 2RTT，并行只需 1RTT。

**今日任务功能**：
- 自动筛选 status=0/1 且 dueDate=今天的任务
- "添加今日任务"按钮打开模态框，从所有未完成任务中选择
- 选择后批量更新 dueDate 为今天

**优先级分布功能**：
- 按优先级（低/中/高）统计任务数量
- 点击某个优先级可查看该优先级的任务列表

**Mock 数据降级**：
```typescript
if (allTasks.length > 0) {
    setTasks(allTasks)  // 使用真实数据
} else {
    const mock = generateMockTasks()  // 使用模拟数据
    setTasks(mock)
}
```

### 6.3 任务管理（Tasks.tsx）

**功能**：筛选 + 列表 + 分页 + AI拆解 + 批量操作

**筛选条件**：状态、优先级、关键词、标签、日期范围

**分页实现**：
```typescript
const [currentPage, setCurrentPage] = useState(1)
const [pageSize, setPageSize] = useState(10)
const totalPages = Math.ceil(tasks.length / pageSize)
const paginatedTasks = tasks.slice((currentPage - 1) * pageSize, currentPage * pageSize)
```

**为什么前端分页而不是后端分页？**

> 当前从后端获取所有任务（size=500），前端做分页。好处是切换页码不需要请求后端，体验更流畅。如果任务量很大（>1000），应该改为后端分页，每次只请求当前页的数据。

**批量操作**：
- 勾选多个任务 → 批量完成/批量删除
- 调用 POST /tasks/batch 接口

**AI 拆解**：
- 点击"AI 拆解"按钮 → 打开 AIDecomposeModal
- 输入任务描述 → AI 生成子任务建议
- 选择要创建的子任务 → 批量创建

### 6.4 番茄钟（Pomodoro.tsx）

**功能**：计时器 + 专注记录 + 任务关联

**计时器实现**：
```typescript
const [timeLeft, setTimeLeft] = useState(25 * 60)
const [isRunning, setIsRunning] = useState(false)
const timerRef = useRef<ReturnType<typeof setInterval> | null>(null)

startTimer = () => {
    // 1. 调用 POST /pomodoro/start 创建记录
    // 2. 启动 setInterval 每秒减1
    timerRef.current = setInterval(() => {
        setTimeLeft((prev) => prev - 1)
    }, 1000)
}

useEffect(() => {
    if (isRunning && timeLeft === 0) {
        // 1. 调用 POST /pomodoro/{id}/end 标记完成
        // 2. 切换到休息模式
    }
}, [timeLeft, isRunning])
```

**关键设计**：
- 工作时长可选：15/25/45/60 分钟
- 休息时长可选：5/10/15 分钟
- 可关联任务：专注时选择一个任务
- 中断处理：调用 /pomodoro/{id}/end 标记为打断
- 使用 useRef 保存 interval 引用，组件卸载时清理

### 6.5 数据统计（Statistics.tsx）

**功能**：本周专注时长 + 总专注次数 + 任务完成趋势 + 优先级分布 + 标签频率

**图表实现**：使用 Recharts 的 BarChart 组件

```typescript
<ResponsiveContainer width="100%" height={300}>
  <BarChart data={weeklyData} barCategoryGap={12}>
    <CartesianGrid strokeDasharray="3 3" stroke="#e8e2d9" />
    <XAxis dataKey="date" />
    <YAxis />
    <Tooltip />
    <Legend />
    <Bar dataKey="completed" name="已完成" fill="#6aab73" radius={[6, 6, 0, 0]} />
    <Bar dataKey="created" name="新建" fill="#5b8a72" radius={[6, 6, 0, 0]} />
  </BarChart>
</ResponsiveContainer>
```

**Mock 数据降级**：API 失败时使用随机生成的模拟数据

### 6.6 系统设置（Settings.tsx）

**功能**：主题切换 + 主题色选择 + 数据导出 + 通知设置

**主题切换**：
- 亮色/暗色模式：Switch 组件 + localStorage 持久化
- 主题色：10 种预设颜色，点击切换

**数据导出**：
- CSV 导出：BOM 头 + 双引号转义 + 标签分号分隔
- JSON 导出：格式化输出

**通知设置**：
- 调用浏览器 Notification API 请求权限
- 权限被拒绝时自动关闭开关

### 6.7 个人中心（Profile.tsx）

**功能**：用户信息展示 + 编辑

**信息展示**：
- 头像、用户名、账号状态
- 注册时间、注册天数
- 邮箱

**编辑功能**：
- 点击编辑按钮进入编辑模式
- 可修改用户名和邮箱
- 调用 PUT /user/update 更新

---

## 七、API 接口设计

### 7.1 认证接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/auth/login | 登录 | 否 |
| POST | /api/auth/register | 注册 | 否 |
| POST | /api/auth/refresh | 刷新Token | 否 |
| POST | /api/auth/logout | 登出 | 是 |

### 7.2 任务接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/tasks | 任务列表（支持筛选+分页） | 是 |
| GET | /api/tasks/{id} | 任务详情 | 是 |
| POST | /api/tasks | 创建任务 | 是 |
| PUT | /api/tasks/{id} | 更新任务 | 是 |
| DELETE | /api/tasks/{id} | 删除任务（软删除） | 是 |
| POST | /api/tasks/batch | 批量操作 | 是 |
| POST | /api/tasks/batch-create | 批量创建 | 是 |

### 7.3 AI 接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/ai/decompose | AI任务拆解 | 是 |

### 7.4 统计接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/statistics/overview | 统计概览 | 是 |

### 7.5 番茄钟接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/pomodoro/start | 开始专注 | 是 |
| POST | /api/pomodoro/{id}/end | 结束专注 | 是 |
| GET | /api/pomodoro/history | 专注记录 | 是 |

### 7.6 用户接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/user/me | 获取个人信息 | 是 |
| PUT | /api/user/update | 更新个人信息 | 是 |

---

## 八、项目难点与解决方案

### 难点 1：并发请求下的 Token 刷新

**问题**：多个请求同时 401，如果每个都发起刷新请求，会导致重复刷新、Token 竞争。

**解决方案**：使用 isRefreshing 标志位 + refreshSubscribers 队列。第一个 401 请求负责刷新，其他请求排队等待，刷新成功后统一用新 Token 重发。

### 难点 2：AI 返回格式不稳定

**问题**：大模型可能返回 Markdown 包裹的 JSON，或格式不合法的 JSON。

**解决方案**：解析前先去除 Markdown 标记，用 Jackson 的 path() 方法安全解析，解析失败走降级策略。

### 难点 3：统计数据实时性与性能的平衡

**问题**：统计数据计算量大，每次请求都计算太慢；但用户操作后要立刻看到变化。

**解决方案**：Cache-Aside 模式 + 主动失效。读时缓存，写时删缓存，TTL 兜底。

### 难点 4：JWT 登出后 Token 仍可用

**问题**：JWT 无状态，签发后无法撤回。用户登出后，Token 在有效期内仍可使用。

**解决方案**：Redis 黑名单。登出时将 Token 的 jti 存入 Redis，TTL 设为剩余有效期。每次验证 Token 时检查黑名单。

### 难点 5：Docker 容器启动顺序

**问题**：后端启动时 MySQL 可能还没准备好，导致连接失败。

**解决方案**：MySQL 配置 healthcheck，后端 depends_on 设置 condition: service_healthy，确保 MySQL 就绪后才启动后端。

---

## 九、安全防护措施

### 9.1 认证安全

| 措施 | 说明 |
|------|------|
| BCrypt 密码加密 | 自适应加盐哈希，防彩虹表攻击 |
| JWT 双 Token | AccessToken 短期 + RefreshToken 长期 |
| Redis 黑名单 | 登出后 Token 立即失效 |
| Token 类型校验 | RefreshToken 不能访问 API |
| 用户状态检查 | 刷新 Token 时检查用户是否被禁用 |

### 9.2 接口安全

| 措施 | 说明 |
|------|------|
| Spring Security 过滤器链 | 所有 /api/** 接口需认证 |
| 权限校验 | Service 层校验数据归属，防越权 |
| 全局异常处理 | 不泄露内部信息 |
| 参数校验 | @Valid + BindException 处理 |

### 9.3 前端安全

| 措施 | 说明 |
|------|------|
| 路由守卫 | 未登录无法访问页面 |
| Token 存储策略 | AccessToken 内存 + RefreshToken localStorage |
| XSS 防护 | React 默认转义，Ant Design 组件安全 |
| CSRF 防护 | 不用 Cookie，用 Bearer Token |

---

## 十、模拟面试 Q&A

### 基础问题

**Q：简单介绍一下你的项目？**

> 这是一个全栈待办清单应用，核心功能包括任务管理、AI 智能拆解、番茄钟专注和数据统计。技术栈是 React 19 + Spring Boot 3.2 + MySQL + Redis，支持 Docker 一键部署。项目的核心亮点是 JWT 双 Token 认证、前端无感刷新、AI 降级策略和 Redis 缓存。

**Q：项目有多少张表？表之间的关系？**

> 3 张表：sys_user、todo_task、pomodoro_record。用户和任务是一对多，任务和番茄钟记录也是一对多。任务表支持自关联（parent_id），用于 AI 拆解后的子任务。

**Q：项目中你负责哪些模块？**

> 我是独立开发，负责全部模块。包括前端页面、后端 API、数据库设计、Docker 部署。

### 技术深度问题

**Q：JWT 和 Session 的区别？各有什么优缺点？**

> | 维度 | JWT | Session |
> |------|-----|---------|
> | 存储位置 | 客户端（Token 本身携带信息） | 服务端（内存/Redis/数据库） |
> | 扩展性 | 天然支持分布式，无需共享状态 | 需要Session共享（Redis/粘性会话） |
> | 安全性 | 无法主动失效（需黑名单机制） | 可随时销毁Session |
> | 性能 | 无需查询存储，验证签名即可 | 每次请求需查Session存储 |
> | 跨域 | 天然支持（Bearer Token） | 需要额外处理（CORS + Cookie） |
> | 适用场景 | 前后端分离、微服务、移动端 | 传统服务端渲染、单体应用 |

> JWT 适合前后端分离架构，但最大缺点是无法主动失效。本项目通过 Redis 黑名单弥补了这个缺陷。Session 方案在分布式环境下需要额外处理 Session 共享问题，增加了系统复杂度。

**Q：BCrypt 和 MD5 加密密码的区别？**

> | 维度 | BCrypt | MD5 |
> |------|--------|-----|
> | 安全性 | 高（自适应加盐+慢哈希） | 低（已被破解，彩虹表攻击） |
> | 速度 | 故意慢（可调节cost参数） | 极快（反而被暴力破解） |
> | 盐值 | 自动生成并内嵌在哈希值中 | 需要手动加盐 |
> | 输出长度 | 固定60字符 | 固定32字符（128bit） |
> | 验证方式 | BCrypt.checkpw(明文, 哈希) | MD5(明文) == 哈希 |

> BCrypt 的核心优势是"自适应成本因子"（cost factor），默认10意味着每次哈希需要 2^10 = 1024 次内部迭代。随着硬件性能提升，可以增大 cost 因子保持破解难度。Spring Security 默认使用 BCryptPasswordEncoder，cost 为 10。

**Q：什么是 XSS 攻击？你的项目怎么防的？**

> XSS（跨站脚本攻击）是攻击者向页面注入恶意脚本。分三种类型：
> - **存储型XSS**：恶意脚本存入数据库，其他用户浏览时执行（危害最大）
> - **反射型XSS**：恶意脚本在URL参数中，服务端原样返回页面执行
> - **DOM型XSS**：前端JS直接操作DOM时注入

> 本项目的防护措施：
> 1. React 默认对 JSX 中的表达式做 HTML 转义（`<` → `&lt;`），防止脚本注入
> 2. 不使用 `dangerouslySetInnerHTML`，避免绕过转义
> 3. Ant Design 组件内部做了 XSS 防护
> 4. 后端全局异常处理不返回堆栈信息，防止信息泄露
> 5. Token 不存在 Cookie 中，避免被 XSS 窃取后发起 CSRF 攻击

**Q：什么是 CSRF 攻击？为什么你的项目不需要防 CSRF？**

> CSRF（跨站请求伪造）是攻击者诱导用户在已认证的网站上执行非预期操作。比如用户已登录银行网站，访问恶意页面后自动发起转账请求（浏览器自动携带Cookie）。

> 本项目不需要防 CSRF 的原因：
> 1. 认证方式是 Bearer Token（放在 Authorization Header），不是 Cookie
> 2. 浏览器不会自动在跨域请求中携带 Authorization Header
> 3. 攻击者无法获取 Token（受同源策略限制），也就无法伪造请求
> 4. Spring Security 配置中显式禁用了 CSRF：`csrf(AbstractHttpConfigurer::disable)`

**Q：如果让你重新设计这个项目，你会做哪些改进？**

> 1. **后端分页**：当前前端分页，任务量大时性能差，改为后端分页+游标分页
> 2. **WebSocket 实时通知**：任务截止日期提醒、番茄钟完成通知，替代轮询
> 3. **消息队列**：AI 拆解改为异步（MQ），避免长时间阻塞请求线程
> 4. **缓存预热**：应用启动时预加载热点数据，避免冷启动时缓存未命中导致数据库压力
> 5. **接口限流**：用 Redis + 令牌桶算法，防止 AI 接口被恶意调用
> 6. **日志链路追踪**：用 MDC + TraceId 串联请求日志，方便排查问题
> 7. **单元测试**：补充 Service 层和 Controller 层的单元测试，覆盖率目标 80%+
> 8. **CI/CD**：GitHub Actions 自动构建、测试、部署
> 9. **监控告警**：Prometheus + Grafana 监控应用指标，异常时告警
> 10. **标签系统重构**：从 JSON 字段改为关联表，支持标签统计和去重查询

**Q：你的项目遇到过什么线上问题？怎么排查的？**

> 开发过程中遇到过几个典型问题：
>
> 1. **Token 刷新死循环**：最初刷新请求也用了 request 实例，导致 401 后再次触发刷新。排查方式是在浏览器 Network 面板看到连续的 /auth/refresh 请求。修复方法是刷新请求用原生 axios 实例。
>
> 2. **Docker 容器启动顺序问题**：后端在 MySQL 未就绪时启动，连接失败直接退出。排查方式是 `docker logs backend` 看到 Connection refused。修复方法是添加 healthcheck + depends_on condition。
>
> 3. **统计数据缓存与实时性不一致**：用户完成任务后仪表盘没更新。排查方式是在 Redis CLI 中手动查看缓存 key。修复方法是在任务增删改的 Service 方法中调用 invalidateCache()。
>
> 4. **前端分页状态丢失**：筛选条件变化后页码没有重置，导致空白页。修复方式是在筛选条件变化时重置 currentPage 为 1。

**Q：React 19 有什么新特性？你用到了哪些？**

> React 19 主要新特性：
> - **自动批处理增强**：异步代码中的状态更新也会自动批处理（React 18 只在同步代码中批处理）
> - **并发渲染**：`useTransition`、`useDeferredValue` 等并发特性稳定
> - **Server Components**：服务端组件（本项目未使用，前后端分离架构）
> - **Actions**：简化表单提交和数据变更
> - **use() Hook**：在渲染期间读取 Promise 和 Context
> - **ref 作为 prop**：函数组件可以直接接收 ref，不再需要 forwardRef
>
> 本项目主要利用了 React 19 的自动批处理和并发渲染能力，配合 Zustand 的 selector 订阅机制，减少了不必要的重渲染。

**Q：Zustand 和 Redux 的核心区别是什么？**

> | 维度 | Zustand | Redux |
> |------|---------|-------|
> | 代码量 | 极少（一个create函数） | 多（action/reducer/store/saga） |
> | 性能 | selector精确订阅，只重渲染用到的字段 | connect或useSelector，需要浅比较 |
> | 异步处理 | 直接在action中写async/await | 需要redux-thunk或redux-saga中间件 |
> | TypeScript | 类型推导自然，无需额外类型定义 | 需要大量类型声明（ActionType等） |
> | 学习曲线 | 低，API只有create/getState/setState | 高，概念多（dispatch/reducer/middleware） |
> | 包体积 | ~1KB | ~7KB（+react-redux ~15KB） |
> | React绑定 | 可在组件外使用（getState()） | 必须在Provider内使用 |
>
> 关键差异：Zustand 的 store 是独立的状态容器，不依赖 React Context，所以可以在 Axios 拦截器等非组件场景中使用 `getState()`。Redux 必须在 `<Provider>` 内使用 hooks，组件外无法访问 store。

**Q：MyBatis-Plus 的分页插件原理？**

> MyBatis-Plus 分页插件基于 MyBatis 的 Interceptor 机制：
> 1. 实现 `Interceptor` 接口，拦截 Executor 的 query 方法
> 2. 解析原始 SQL，改写为 `SELECT COUNT(*)` 查询总数
> 3. 再改写为 `SELECT ... LIMIT offset, size` 查询分页数据
> 4. 将总数和分页数据封装为 Page 对象返回
>
> 底层使用 JSqlParser 解析和改写 SQL，支持 MySQL、PostgreSQL、Oracle 等多种数据库方言。本项目暂时使用前端分页，但 MyBatis-Plus 分页插件已配置好，随时可切换到后端分页。

**Q：Spring Boot 3.2 相比 2.x 有什么重大变化？**

> 1. **最低要求 Java 17**：从 Java 8 升级到 Java 17，支持 Record、Sealed Class、文本块等
> 2. **Jakarta EE 替代 javax**：包名从 `javax.*` 迁移到 `jakarta.*`（如 `jakarta.servlet`）
> 3. **Spring Security 6**：配置方式从 `WebSecurityConfigurerAdapter` 改为 `SecurityFilterChain` Bean
> 4. **原生编译支持**：支持 GraalVM AOT 编译为原生可执行文件，启动时间毫秒级
> 5. **Observability**：内置 Micrometer，支持分布式追踪和指标采集
> 6. **虚拟线程**：Java 21 的虚拟线程支持，大幅提升并发性能
>
> 本项目使用 Spring Boot 3.2，配置 Security 时用的是新版的 `SecurityFilterChain` Bean 方式，不再继承 `WebSecurityConfigurerAdapter`。

**Q：Redis 的数据结构有哪些？你在项目中用了哪些？**

> Redis 有 5 种基础数据结构 + 3 种扩展结构：
>
> | 结构 | 说明 | 本项目用途 |
> |------|------|-----------|
> | String | 最基础，可存字符串/数字/JSON | Token黑名单、统计数据缓存 |
> | Hash | 字段-值映射 | 未使用（可用于存储用户Session） |
> | List | 有序列表 | 未使用（可用于消息队列） |
> | Set | 无序去重集合 | 未使用（可用于标签去重） |
> | ZSet | 有序去重集合 | 未使用（可用于排行榜） |
> | HyperLogLog | 基数估算 | 未使用 |
> | Bitmap | 位操作 | 未使用 |
> | Geo | 地理位置计算 | 未使用 |
>
> 本项目主要用了 String 类型：
> - `token:blacklist:{jti}` → "1"（Token黑名单，TTL为Token剩余有效期）
> - `statistics:overview:{userId}` → JSON字符串（统计数据缓存，TTL 5分钟）

**Q：如果用户量增长到百万级，你的系统会有什么瓶颈？怎么优化？**

> 1. **数据库瓶颈**：单表数据量过大，查询变慢
>    - 优化：读写分离（主从复制）、分库分表（按用户ID分片）、热点数据缓存
> 2. **Redis 瓶颈**：单节点内存和带宽限制
>    - 优化：Redis Cluster 分片、读写分离、本地缓存（Caffeine）+ Redis 二级缓存
> 3. **后端瓶颈**：单机并发能力有限
>    - 优化：水平扩展（多实例 + Nginx 负载均衡）、异步处理（MQ）、连接池优化
> 4. **AI 接口瓶颈**：DeepSeek API 调用频率限制
>    - 优化：请求队列 + 限流、结果缓存（相同任务不重复调用）、异步处理
> 5. **前端瓶颈**：大量任务渲染卡顿
>    - 优化：虚拟列表（react-window）、后端分页、懒加载

**Q：什么是软删除？和物理删除的优缺点？**

> | 维度 | 软删除 | 物理删除 |
> |------|--------|---------|
> | 实现 | is_deleted字段标记 | DELETE FROM直接删除 |
> | 可恢复 | 可恢复（改回is_deleted=0） | 不可恢复 |
> | 存储空间 | 占用空间持续增长 | 释放空间 |
> | 查询性能 | 需要WHERE is_deleted=0过滤 | 无额外条件 |
> | 唯一约束 | 需要特殊处理（已删除的记录仍占唯一值） | 无影响 |
> | 数据一致性 | 需要定时清理 | 无需额外处理 |

> 本项目采用两阶段删除：先软删除（可恢复），30天后定时物理删除（释放空间）。这是最常见的做法，兼顾了用户体验和存储效率。

**Q：Docker Compose 的 depends_on 能保证服务就绪吗？**

> 不能。`depends_on` 只保证启动顺序，不保证服务就绪。比如 MySQL 容器启动了但还在初始化，此时后端连接会失败。必须配合 `healthcheck` + `condition: service_healthy` 才能确保依赖服务真正可用。

> ```yaml
> # 只保证启动顺序（不够）
> depends_on:
>   - mysql
>
> # 保证服务就绪（推荐）
> depends_on:
>   mysql:
>     condition: service_healthy
> ```

**Q：Nginx 的 try_files 什么意思？为什么 SPA 需要它？**

> `try_files $uri $uri/ /index.html` 的含义：先尝试按文件路径找，再按目录找，都找不到则返回 index.html。

> SPA（单页应用）所有路由都由前端 JavaScript 处理。用户访问 `/tasks` 时，Nginx 会去找 `/tasks` 对应的文件，但实际不存在这个文件（只有 index.html）。如果不配置 try_files，Nginx 会返回 404。配置后，Nginx 返回 index.html，前端路由接管，正确渲染 Tasks 页面。

> 但这也有副作用：真正的 404（如错误的 JS/CSS 文件路径）也会返回 index.html。生产环境可以通过区分 `/api` 和静态资源路径来避免。

---

## 十一、前端架构与组件设计详解

### 11.1 前端目录结构

```
ai-todo-frontend/src/
├── App.tsx                    # 根组件：主题配置 + 路由
├── main.tsx                   # 入口文件
├── utils/
│   └── request.ts             # Axios 实例 + 拦截器（无感刷新）
├── store/
│   ├── authStore.ts           # 认证状态（Zustand + persist）
│   └── taskStore.ts           # 任务状态（Zustand，不持久化）
├── pages/
│   ├── Login.tsx              # 登录/注册
│   ├── Dashboard.tsx          # 仪表盘
│   ├── Tasks.tsx              # 任务管理
│   ├── Pomodoro.tsx           # 番茄钟
│   ├── Statistics.tsx         # 数据统计
│   ├── Settings.tsx           # 系统设置
│   └── Profile.tsx            # 个人中心
├── components/
│   ├── Layout.tsx             # 布局组件（侧边栏+顶栏+Outlet）
│   └── ai/
│       └── AIDecomposeModal.tsx  # AI拆解模态框
└── types/
    └── index.ts               # TypeScript 类型定义
```

### 11.2 组件通信方式

本项目使用了以下组件通信方式：

| 通信方式 | 使用场景 | 代码示例 |
|---------|---------|---------|
| Props 传递 | 父→子组件数据传递 | `<TaskCard task={task} onUpdate={handleUpdate} />` |
| Zustand Store | 跨组件共享状态 | `useAuthStore.getState().token` |
| CustomEvent | 主题切换通信 | `window.dispatchEvent(new CustomEvent('themeChange'))` |
| 回调函数 | 子→父组件事件通知 | `onComplete={(id) => updateTask(id, {status: 2})}` |
| URL 参数 | 页面间传参 | `navigate('/tasks?priority=2')` |

**为什么主题切换用 CustomEvent 而不是 Zustand？**

> 主题状态在 App.tsx 的 ConfigProvider 中使用，而切换操作在 Settings.tsx 中。如果用 Zustand，App.tsx 需要订阅 store 变化触发重渲染，而 ConfigProvider 的 theme 属性变化需要整个组件树重渲染。CustomEvent 更轻量，只在需要时触发更新。

### 11.3 前端性能优化实践

**1. Zustand selector 精确订阅**

```typescript
// ❌ 错误：订阅整个store，任何字段变化都触发重渲染
const store = useAuthStore()

// ✅ 正确：只订阅需要的字段
const token = useAuthStore((state) => state.token)
const user = useAuthStore((state) => state.user)
```

**2. React.memo 避免不必要的重渲染**

```typescript
const TaskCard = React.memo(({ task, onUpdate }: TaskCardProps) => {
    return <Card>...</Card>
})
```

**3. 前端分页减少 DOM 节点**

```typescript
const paginatedTasks = tasks.slice((currentPage - 1) * pageSize, currentPage * pageSize)
// 只渲染当前页的10条，而不是全部500条
```

**4. Promise.all 并行请求**

```typescript
const [tasksRes, pomodoroRes] = await Promise.all([
    request.get('/tasks?page=1&size=500'),
    request.get('/pomodoro/history'),
])
```

**5. useEffect 清理副作用**

```typescript
useEffect(() => {
    const timer = setInterval(() => { setTimeLeft(prev => prev - 1) }, 1000)
    return () => clearInterval(timer)  // 组件卸载时清理定时器
}, [])
```

**6. Blob URL 及时释放**

```typescript
const url = URL.createObjectURL(blob)
link.click()
URL.revokeObjectURL(url)  // 释放内存
```

### 11.4 前端错误处理策略

```
API 请求错误
├── 网络错误（无响应）→ message.error('网络错误')
├── 401 未认证 → 自动刷新Token / 跳转登录
├── 403 无权限 → message.error('无权限')
├── 400 参数错误 → 显示后端返回的具体错误信息
├── 500 服务器错误 → message.error('系统错误')
└── 超时 → message.error('请求超时')

组件渲染错误
└── ErrorBoundary 捕获 → 显示降级UI
```

---

## 十二、后端架构与设计模式详解

### 12.1 后端目录结构

```
ai-todo-backend/src/main/java/com/aitodo/
├── AITodoApplication.java          # 启动类
├── config/
│   ├── SecurityConfig.java         # Spring Security 配置
│   ├── RedisConfig.java            # Redis 配置
│   ├── WebConfig.java              # CORS 跨域配置
│   └── MyBatisPlusConfig.java      # MyBatis-Plus 配置（分页插件等）
├── security/
│   ├── JwtService.java             # JWT 生成/解析/验证/黑名单
│   └── JwtAuthenticationFilter.java # JWT 认证过滤器
├── controller/
│   ├── AuthController.java         # 认证接口
│   ├── TaskController.java         # 任务接口
│   ├── AIController.java           # AI 接口
│   ├── StatisticsController.java   # 统计接口
│   ├── PomodoroController.java     # 番茄钟接口
│   └── UserController.java         # 用户接口
├── service/
│   ├── AuthService.java            # 认证业务
│   ├── TaskService.java            # 任务业务
│   ├── AIService.java              # AI 业务
│   ├── StatisticsService.java      # 统计业务
│   ├── PomodoroService.java        # 番茄钟业务
│   └── UserService.java            # 用户业务
├── mapper/
│   ├── UserMapper.java             # 用户 Mapper
│   ├── TaskMapper.java             # 任务 Mapper
│   └── PomodoroMapper.java         # 番茄钟 Mapper
├── entity/
│   ├── User.java                   # 用户实体
│   ├── Task.java                   # 任务实体
│   └── PomodoroRecord.java         # 番茄钟记录实体
├── dto/
│   ├── request/                    # 请求 DTO
│   │   ├── LoginRequest.java
│   │   ├── RegisterRequest.java
│   │   ├── TaskRequest.java
│   │   ├── AIDecomposeRequest.java
│   │   └── BatchTaskRequest.java
│   └── response/                   # 响应 DTO
│       ├── LoginResponse.java
│       ├── TaskResponse.java
│       ├── AIDecomposeResponse.java
│       └── StatisticsResponse.java
├── common/
│   ├── Result.java                 # 统一响应封装
│   └── exception/
│       ├── BusinessException.java  # 业务异常
│       └── GlobalExceptionHandler.java # 全局异常处理
└── schedule/
    └── TaskCleanupJob.java         # 定时清理任务
```

### 12.2 分层架构与职责

```
Controller 层
├── 职责：接收请求、参数校验、调用Service、返回响应
├── 注解：@RestController、@RequestMapping、@Valid
├── 规范：不写业务逻辑，只做参数转换和结果封装
└── 返回值：统一用 Result<T> 封装

Service 层
├── 职责：业务逻辑、事务管理、缓存管理
├── 注解：@Service、@Transactional
├── 规范：业务逻辑都在这里，包括权限校验、缓存失效
└── 特点：靠近数据层做安全校验

Mapper 层
├── 职责：数据访问，SQL执行
├── 继承：BaseMapper<T>（MyBatis-Plus 通用 CRUD）
├── 规范：简单查询用 MyBatis-Plus 的 LambdaQueryWrapper
└── 复杂查询：自定义 SQL + @Select 注解
```

### 12.3 设计模式应用

**1. 策略模式——AI 降级策略**

```java
// 根据不同关键词选择不同的拆解策略
if (lowerTask.contains("面试")) {
    return interviewTemplate();
} else if (lowerTask.contains("学习")) {
    return studyTemplate();
} else {
    return genericTemplate();
}
```

如果后续要支持更多策略，可以抽象为策略接口：

```java
public interface DecomposeStrategy {
    boolean supports(String task);
    List<SubTask> decompose(String task);
}
```

**2. 模板方法模式——StatisticsService**

```java
public StatisticsResponse getOverview() {
    // 模板：查缓存 → 未命中则计算 → 回填缓存
    String cached = redisTemplate.opsForValue().get(cacheKey);
    if (cached != null) return deserialize(cached);
    StatisticsResponse response = buildStatistics(userId);  // 子类可重写
    redisTemplate.opsForValue().set(cacheKey, serialize(response), 5, TimeUnit.MINUTES);
    return response;
}
```

**3. 建造者模式——Response 对象**

```java
return LoginResponse.builder()
    .accessToken(accessToken)
    .refreshToken(refreshToken)
    .user(userInfo)
    .build();
```

Lombok 的 `@Builder` 注解生成建造者模式代码，链式调用清晰可读。

**4. 责任链模式——Spring Security 过滤器链**

```
CorsFilter → CsrfFilter → SessionManagement → JwtAuthenticationFilter → AuthorizationFilter
```

每个过滤器只处理自己关心的逻辑，处理完传递给下一个。

**5. 观察者模式——缓存失效**

```java
public Task createTask(TaskRequest request) {
    taskMapper.insert(task);
    statisticsService.invalidateCache();  // 通知统计服务缓存失效
    return task;
}
```

任务变更时主动通知统计服务删除缓存，类似观察者模式中的事件通知。

**6. 单例模式——Spring Bean**

所有 `@Service`、`@Component`、`@RestController` 默认都是单例。Spring 容器保证线程安全的方式是无状态设计（不在 Bean 中存储可变实例变量）。

### 12.4 统一响应封装

```java
@Data
@Builder
public class Result<T> {
    private int code;
    private String message;
    private T data;

    public static <T> Result<T> success(T data) {
        return Result.<T>builder().code(200).message("success").data(data).build();
    }

    public static <T> Result<T> error(int code, String message) {
        return Result.<T>builder().code(code).message(message).build();
    }
}
```

**为什么统一封装？**

> 1. 前端可以根据 code 统一处理错误（拦截器中判断 code !== 200）
> 2. 接口文档格式统一，降低前后端沟通成本
> 3. 全局异常处理可以统一返回格式，不会出现有的接口返回裸数据、有的返回包装对象

### 12.5 DTO 与 Entity 分离

```
请求流程：Client → Request DTO → Controller → Service → Entity → Mapper → DB
响应流程：DB → Mapper → Entity → Service → Response DTO → Controller → Client
```

**为什么分离？**

> 1. **安全性**：Entity 包含所有字段（如 password），直接返回会泄露敏感信息
> 2. **灵活性**：请求和响应的字段可能不同（如创建时不需要 id，响应时需要）
> 3. **验证**：Request DTO 可以加 `@Valid` 注解做参数校验，Entity 不需要
> 4. **版本兼容**：API 版本升级时，DTO 可以做兼容处理，Entity 跟随数据库变化

---

## 十三、性能优化专题

### 13.1 后端性能优化

**1. Redis 缓存——统计数据**

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 统计接口响应时间 | 200-500ms | 2-5ms | 100倍 |
| 数据库查询次数 | 每次请求3-5次 | 5分钟内0次 | 显著降低 |

**2. MyBatis-Plus 优化**

```java
// 使用 LambdaQueryWrapper 避免硬编码字段名
new LambdaQueryWrapper<Task>()
    .eq(Task::getUserId, userId)
    .eq(Task::getIsDeleted, 0)
    .orderByDesc(Task::getCreatedAt);

// 批量操作用 updateBatchById 而不是循环单条更新
taskMapper.updateBatchById(tasks);
```

**3. 连接池配置**

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20        # 最大连接数
      minimum-idle: 5              # 最小空闲连接
      connection-timeout: 30000    # 连接超时30秒
      idle-timeout: 600000         # 空闲超时10分钟
      max-lifetime: 1800000        # 连接最大存活30分钟
```

**为什么用 HikariCP？**

> HikariCP 是 Spring Boot 默认连接池，号称"零开销"代理。核心优化：FastList 替代 ArrayList、ConcurrentBag 无锁并发、字节码级优化。相比 Druid，HikariCP 更轻量、性能更高。Druid 的优势是监控功能丰富，适合需要详细监控的场景。

### 13.2 前端性能优化

**1. Vite 构建优化**

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],       // 框架单独打包
          antd: ['antd'],                        // UI库单独打包
          charts: ['recharts'],                  // 图表库单独打包
        }
      }
    }
  }
})
```

**分包策略的好处**：
- vendor/antd/charts 变化少，浏览器缓存命中率高
- 业务代码变化时，用户只需重新下载业务包
- 首屏加载可以按需加载图表库

**2. 组件懒加载**

```typescript
const Statistics = React.lazy(() => import('./pages/Statistics'))
const Pomodoro = React.lazy(() => import('./pages/Pomodoro'))

<Suspense fallback={<Spin />}>
  <Routes>
    <Route path="/statistics" element={<Statistics />} />
    <Route path="/pomodoro" element={<Pomodoro />} />
  </Routes>
</Suspense>
```

**3. Nginx 静态资源缓存**

```nginx
location ~* \.(js|css|png|jpg|svg|woff2)$ {
    expires 1y;                    # 强缓存1年
    add_header Cache-Control "public, immutable";
}
location / {
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "no-cache";  # HTML不缓存
}
```

**为什么 JS/CSS 缓存1年但 HTML 不缓存？**

> Vite 构建后 JS/CSS 文件名带 hash（如 `index-a1b2c3.js`），内容变化时文件名也变，可以放心长期缓存。但 index.html 不带 hash，如果被缓存，用户永远加载旧版本的 JS/CSS 引用。所以 HTML 必须不缓存或短缓存。

**4. Gzip 压缩**

```nginx
gzip on;
gzip_types text/plain text/css application/json application/javascript;
gzip_min_length 1024;
```

Gzip 压缩后，JS/CSS 体积减少 60-80%，显著加快传输速度。

### 13.3 数据库性能优化

**1. 索引优化**

```sql
-- 任务列表最常用查询
SELECT * FROM todo_task WHERE user_id = ? AND status = ? AND is_deleted = 0
-- 对应复合索引 idx_user_status (user_id, status, is_deleted)
-- 覆盖索引，无需回表
```

**2. 避免全表扫描**

```java
// ❌ 错误：没有条件，全表扫描
taskMapper.selectList(null);

// ✅ 正确：带条件查询，走索引
taskMapper.selectList(new LambdaQueryWrapper<Task>()
    .eq(Task::getUserId, userId)
    .eq(Task::getIsDeleted, 0));
```

**3. JSON 字段查询优化**

```sql
-- tags 是 JSON 字段，LIKE 查询不走索引
SELECT * FROM todo_task WHERE tags LIKE '%"工作%"'
-- 如果标签查询频繁，考虑新增关联表 + 索引
```

**4. 批量操作优化**

```java
// ❌ 错误：循环单条插入，N次网络往返
for (SubTask subTask : subTasks) {
    taskMapper.insert(task);
}

// ✅ 正确：批量插入，1次网络往返
taskMapper.insertBatchSomeColumn(taskList);
// 或 MyBatis-Plus 的 saveBatch
taskService.saveBatch(taskList, 500);  // 每500条一批
```

---

## 十四、部署与运维详解

### 14.1 Docker Compose 完整配置解析

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: ai_todo
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 20s
      retries: 10
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./ai-todo-backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/ai_todo
      SPRING_REDIS_HOST: redis

  frontend:
    build:
      context: ./ai-todo-frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
  redis_data:
```

**关键配置详解**：

| 配置项 | 说明 | 面试考点 |
|--------|------|---------|
| healthcheck | MySQL健康检查 | 保证后端等MySQL就绪才启动 |
| depends_on condition | 服务依赖条件 | 区别于简单depends_on |
| volumes 命名卷 | 数据持久化 | 容器删除后数据不丢失 |
| mysql command | 字符集配置 | utf8mb4支持emoji |
| SPRING_DATASOURCE_URL | 环境变量覆盖 | Docker服务名替代localhost |
| redis:7-alpine | Alpine镜像 | 体积更小（~30MB vs ~120MB） |

### 14.2 Dockerfile 多阶段构建

**后端 Dockerfile**：

```dockerfile
# 阶段1：构建
FROM maven:3.9-eclipse-temurin-17 AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline          # 依赖预下载（利用Docker缓存层）
COPY src ./src
RUN mvn package -DskipTests            # 打包

# 阶段2：运行
FROM eclipse-temurin:17-jre-alpine     # 只需JRE，不需要JDK
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**镜像体积对比**：
- 不用多阶段构建：maven + JDK + 源码 + jar ≈ 800MB
- 多阶段构建：JRE-alpine + jar ≈ 200MB
- 减少 75% 镜像体积

**前端 Dockerfile**：

```dockerfile
# 阶段1：构建
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json .
RUN npm ci                              # ci比install更严格，按lock文件安装
COPY . .
RUN npm run build                        # Vite构建，输出到dist/

# 阶段2：运行
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

**为什么用 npm ci 而不是 npm install？**

> `npm ci` 严格按照 `package-lock.json` 安装，不会更新依赖版本，构建结果可复现。`npm install` 可能更新依赖，导致不同时间构建出不同结果。

### 14.3 Nginx 配置详解

```nginx
server {
    listen 80;
    server_name localhost;

    # 前端静态资源
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;  # SPA路由回退
        add_header Cache-Control "no-cache";
    }

    # API反向代理
    location /api {
        proxy_pass http://backend:8080;     # Docker服务名解析
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源长缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 1024;
    gzip_comp_level 6;
}
```

**proxy_set_header 为什么需要设置？**

> 反向代理后，后端获取的 IP 是 Nginx 的 IP，不是客户端真实 IP。通过 `X-Real-IP` 和 `X-Forwarded-For` 传递真实客户端 IP，后端可以通过 `request.getHeader("X-Real-IP")` 获取。

---

## 十五、项目亮点总结与面试展示策略

### 15.1 项目亮点一句话总结

| 序号 | 亮点 | 一句话描述 |
|------|------|-----------|
| 1 | JWT双Token认证 | AccessToken短期+RefreshToken长期+Redis黑名单，安全登出 |
| 2 | 无感Token刷新 | Axios拦截器+请求队列，并发401只刷新一次 |
| 3 | AI智能拆解 | DeepSeek API+System Prompt+三层降级策略 |
| 4 | Redis缓存 | Cache-Aside模式+主动失效，统计接口100倍提速 |
| 5 | Spring Security | 自定义JWT过滤器+无状态会话+路径权限 |
| 6 | 软删除+定时清理 | 两阶段删除，可恢复+自动物理删除 |
| 7 | 批量操作+权限校验 | Service层逐条校验数据归属，防越权 |
| 8 | 全局异常处理 | 统一响应格式+不泄露内部信息 |
| 9 | Zustand状态管理 | persist持久化+组件外getState+精确订阅 |
| 10 | 动态主题 | 10种主题色+暗黑模式+CustomEvent通信 |
| 11 | 数据导出 | CSV(BOM+转义)+JSON，Blob URL及时释放 |
| 12 | Docker一键部署 | 多阶段构建+healthcheck+Nginx反向代理 |

### 15.2 面试展示策略

**1. 用数字说话**

> ❌ "我做了缓存优化"
> ✅ "我用 Redis 缓存统计数据，接口响应从 500ms 降到 5ms，提升了 100 倍"

> ❌ "我做了 Docker 部署"
> ✅ "我用多阶段构建优化 Docker 镜像，体积从 800MB 降到 200MB，减少 75%"

**2. 用对比展示技术选型能力**

> "我对比了 Zustand 和 Redux，最终选择 Zustand。原因是代码量少 70%、包体积小 14 倍、支持组件外访问，更适合中小型项目。"

**3. 用问题展示思考深度**

> "AI 拆解功能最大的挑战是 API 不稳定。我设计了三层降级策略：API 调用 → 关键词匹配预设模板 → 通用模板兜底，保证功能在任何情况下都可用。"

**4. 用架构图展示全局视野**

> 面试时在白板上画出项目架构图：浏览器 → Nginx → Spring Boot → MySQL/Redis/DeepSeek，展示你对系统整体的理解。

### 15.3 项目介绍模板（3分钟版本）

> 我独立开发了一个全栈待办清单应用，核心功能包括任务管理、AI智能拆解、番茄钟专注和数据统计。
>
> 技术栈方面，前端用 React 19 + TypeScript + Ant Design 6 + Zustand，后端用 Spring Boot 3.2 + MyBatis-Plus + Spring Security，数据层 MySQL 8 + Redis 7，部署用 Docker Compose 一键编排。
>
> 项目有四个核心亮点：
>
> 第一，JWT 双 Token 认证体系。AccessToken 短期有效 2 小时，RefreshToken 长期有效 7 天。登出时将 Token 的唯一标识存入 Redis 黑名单，TTL 设为剩余有效期，既保证安全又节省内存。
>
> 第二，前端无感 Token 刷新。通过 Axios 拦截器自动处理 401，用标志位和请求队列保证并发场景下只刷新一次，用户完全无感知。
>
> 第三，AI 智能任务拆解。调用 DeepSeek 大模型 API，通过精心设计的 System Prompt 约定返回格式。更重要的是设计了三层降级策略，API 不可用时根据关键词匹配预设模板，保证功能可用性。
>
> 第四，Redis 缓存 + 主动失效。统计数据用 Cache-Aside 模式缓存，读时回源回填，写时主动删除，接口响应从 500ms 降到 5ms。
>
> 部署方面，用 Docker Compose 编排四个服务，MySQL 配置健康检查保证后端等数据库就绪才启动，Dockerfile 多阶段构建将镜像体积减少 75%。

### 15.4 项目介绍模板（1分钟精简版）

> 我独立开发了一个全栈待办清单应用，前端 React 19，后端 Spring Boot 3.2，数据库 MySQL + Redis。核心亮点是 JWT 双 Token 认证配合 Redis 黑名单实现安全登出、前端无感 Token 刷新、AI 智能任务拆解带三层降级策略、Redis 缓存让统计接口提速 100 倍。部署用 Docker Compose 一键编排，多阶段构建优化镜像体积。

### 15.5 面试常见追问应对

| 追问方向 | 应对策略 |
|---------|---------|
| "这个项目有什么不足？" | 诚实说出改进点（后端分页、MQ异步、监控告警），展示你思考过优化方向 |
| "遇到最大的困难是什么？" | 选一个有技术深度的（并发Token刷新、AI降级策略），展示解决思路 |
| "为什么选这个技术栈？" | 对比其他方案，说出选型理由（Zustand vs Redux、MyBatis-Plus vs JPA） |
| "如果用户量增长100倍？" | 从数据库、缓存、后端、前端四个层面分析瓶颈和优化方案 |
| "线上出过什么问题？" | 说真实遇到的问题和排查过程（Token死循环、Docker启动顺序） |
| "代码量多少？开发周期？" | 诚实回答，强调独立全栈开发和工程化实践 |

---

## 十六、高频面试题速查表

### 16.1 Java 基础

| 题目 | 核心答案 |
|------|---------|
| HashMap 底层原理 | 数组+链表+红黑树，负载因子0.75，链表长度>8转红黑树 |
| ConcurrentHashMap | JDK7分段锁，JDK8 CAS+synchronized，锁粒度更细 |
| JVM 内存模型 | 堆（新生代+老年代）、方法区、虚拟机栈、本地方法栈、程序计数器 |
| 垃圾回收算法 | 标记清除、复制、标记整理、分代收集 |
| 线程池参数 | corePoolSize、maxPoolSize、keepAliveTime、workQueue、handler |
| synchronized 锁升级 | 无锁→偏向锁→轻量级锁→重量级锁 |

### 16.2 Spring 全家桶

| 题目 | 核心答案 |
|------|---------|
| Spring IOC | 控制反转，Bean生命周期：实例化→属性注入→初始化→使用→销毁 |
| Spring AOP | 动态代理（JDK/CGLIB），@Transactional基于AOP实现 |
| @Transactional 失效场景 | 非public方法、自调用、异常类型不匹配、未被Spring管理 |
| Spring Boot 自动配置 | @EnableAutoConfiguration → spring.factories → @Conditional |
| Bean 作用域 | singleton（默认）、prototype、request、session、application |

### 16.3 数据库

| 题目 | 核心答案 |
|------|---------|
| 事务ACID | 原子性、一致性、隔离性、持久性 |
| 事务隔离级别 | 读未提交→读已提交→可重复读（MySQL默认）→串行化 |
| 索引类型 | 主键索引、唯一索引、普通索引、复合索引、全文索引 |
| 索引失效场景 | 函数操作、隐式转换、LIKE左模糊、OR条件、不满足最左前缀 |
| MVCC原理 | 隐藏列+undo log+ReadView，实现无锁读 |
| 慢查询优化 | EXPLAIN分析→加索引→优化SQL→分库分表 |

### 16.4 Redis

| 题目 | 核心答案 |
|------|---------|
| 持久化方式 | RDB（快照）+ AOF（追加日志），混合持久化 |
| 过期删除策略 | 惰性删除+定期删除 |
| 内存淘汰策略 | LRU、LFU、TTL、随机、不淘汰 |
| 缓存击穿/穿透/雪崩 | 击穿：互斥锁；穿透：布隆过滤器；雪崩：随机TTL |
| 分布式锁 | SETNX + 过期时间 + Lua脚本保证原子性 |
| Redis Cluster | 16384个槽位，每个节点负责一部分，Gossip协议通信 |

### 16.5 计算机网络

| 题目 | 核心答案 |
|------|---------|
| TCP 三次握手 | SYN→SYN+ACK→ACK，确保双方收发能力正常 |
| TCP 四次挥手 | FIN→ACK→FIN→ACK，TIME_WAIT等待2MSL |
| HTTP/1.1 vs HTTP/2 | 多路复用、头部压缩、服务端推送、二进制帧 |
| HTTPS | TLS握手→证书验证→密钥协商→加密通信 |
| CORS | 预检请求OPTIONS→Access-Control-Allow-Origin等响应头 |

---

## 十七、面试前最后检查清单

- [ ] 能流畅说出项目3分钟介绍
- [ ] 能画出项目架构图
- [ ] 能解释12大技术亮点的实现细节
- [ ] 能回答"项目不足"和"改进方向"
- [ ] 能回答"遇到最大的困难"
- [ ] 能回答"如果用户量增长100倍"
- [ ] 能解释JWT双Token的完整流程（登录→使用→刷新→登出）
- [ ] 能解释前端无感刷新的并发处理机制
- [ ] 能解释AI降级策略的三层设计
- [ ] 能解释Redis缓存的Cache-Aside模式
- [ ] 能解释Spring Security过滤器链的执行顺序
- [ ] 能解释软删除+定时清理的两阶段设计
- [ ] 能解释Docker Compose的healthcheck机制
- [ ] 能解释Nginx的try_files和反向代理配置
- [ ] 能解释数据库索引设计（复合索引、最左前缀）
- [ ] 能对比JWT vs Session、Zustand vs Redux、MyBatis-Plus vs JPA