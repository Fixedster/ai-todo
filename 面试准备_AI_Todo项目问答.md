# AI Todo 智能待办清单 — 面试准备文档

---

## 项目介绍（一段话）

AI Todo 是一个智能待办清单应用，可以理解为一个"聪明版"的任务管理工具。我独立完成了这个项目的全部开发，包括后端（藏在后台处理数据的程序）、前端（你在浏览器里看到的页面）和部署上线。功能有四个：① 创建和管理任务（标优先级、按状态筛选、搜索）；② AI 帮你拆任务（输入"准备面试"就自动拆分出小步骤）；③ 番茄钟计时器（帮你集中注意力）；④ 数据统计看板（图表展示你的完成情况）。项目用 Docker 一键启动，不管换到哪台电脑都能直接跑起来。

---

## 面试 Q&A

### 1. 为什么用双 Token 方案？

**先解释一下 Token 是什么：**
Token 就像一张"通行证"。你登录成功后，服务器会发给你一张通行证，之后你每次请求数据都要带上它，服务器看到通行证就知道你是谁了，不用每次再输密码。

**单 Token 有什么问题？**
假设我们只发一张通行证，有效期怎么设都不合适：
- 有效期设太短（30分钟）：用户用着用着突然过期了，需要重新登录，体验很差
- 有效期设太长（7天）：如果被别人偷了这张通行证，他可以冒充你长达 7 天

**我们的双 Token 方案：**
我们发两张通行证：
- **小通行证（有效期 2 小时）**：用户每次请求都带上这个，过期了就用另一个去换新的
- **大通行证（有效期 7 天）**：不常用，只在"小通行证过期了，帮我换个新的"时使用

打个比方：去健身房，小通行证是临时的储物柜手环（2 小时有效），大通行证是年卡（7 天有效）。手环过期了，用年卡去前台换一个新的手环就行，不用重新办卡。

**最关键的设计：并发请求的排队刷新**
用户很可能同时发好几个请求，如果小通行证刚好过期，这几个请求都会返回"未登录"错误。我们的处理方式：第一个请求发现过期了，它去换新证；其他请求排着队等着；新证换回来后，排队的请求全部用上新证重新发。好处是"一次刷新，大家受益"。

### 2. 怎么实现退出登录后立即失效？

**问题：**
Token 一旦签发，在有效期内始终有效。但用户点击"退出登录"，我们希望这张通行证立即作废。

**具体做法：**
每张通行证上都有一个唯一的编号。用户退出登录时，把这个编号存到缓存里，记录为"已拉黑"。以后每次请求，服务器检查通行证时，先去缓存看看这个编号在黑名单里没有，如果在就拒绝。

**为什么用缓存？**
- 查得快：毫秒级别
- 自动过期：给黑名单记录设置倒计时，时间等于通行证剩余有效期。比如通行证还有 3 小时过期，黑名单记录 3 小时后自动消失，不会一直存着没用数据

**用日常例子理解：**
健身卡丢了，你去前台挂失。前台记下"这张卡已挂失"。之后有人拿这张卡来，前台一查，"哦，在黑名单里"，不让进。等卡的有效期到了，挂失记录自动删除。

### 3. AI 降级方案具体怎么做的？

**AI 拆解任务是什么？**
比如用户输入"我要准备 Java 面试"，AI 能自动帮忙拆成更小的步骤：复习基础知识、刷算法题、模拟面试练习等。

**问题：如果 AI 服务挂了怎么办？**
项目调用了 DeepSeek 的 AI 接口（外部服务）。如果网络不好、接口超时、返回数据格式不对，用户那边就转圈等半天。

**我们的降级方案：**
后端做了"Plan B"——如果 AI 调用失败，就用关键词匹配的方式，从预设好的模板里选一组最接近的：

```
用户输入"准备 Java 面试"
    ↓
尝试调 AI → 成功了？→ 返回 AI 结果
    ↓（失败）
检查有没有"面试" → 有！→ 返回面试模板
检查有没有"学习" → 返回学习模板
检查有没有"旅行" → 返回旅行模板
都不匹配 → 返回通用模板
```

每种模板都是提前写好的合理建议：
- **面试模板**：准备简历和自我介绍、复习基础知识、刷算法题、模拟面试练习
- **学习模板**：搜集学习资料、制定学习计划、动手实践练习、总结复盘
- **旅行模板**：确定目的地和行程、预订交通和住宿、准备行李
- **通用模板**：明确目标和范围、拆解具体步骤、分配时间和资源、执行并检查进度

**好处：** 即使用的 AI 服务出了故障，用户依然能得到有意义的建议，页面不会卡住或报错。体现了容错意识。

### 4. 任务表的数据库索引怎么设计的？

**什么是索引：**
索引就像书的目录。没有目录，你想找一个知识点得整本书一页页翻。数据库也是这个道理。

**怎么设的：**
1. **最重要的目录**：把"用户ID + 任务状态 + 是否删除"三个条件合在一起。因为用户看任务列表时基本就是"只看我的 + 只看待办的 + 没被删的"，这个目录最快
2. **截止日期目录**：快速找出"今天到期的任务"
3. **优先级目录**：按高/中/低筛选时用
4. **搜索目录**：用户搜关键词时，在标题和描述里快速找到

**标签存在哪？**
任务的标签（如"工作"、"学习"）直接存在任务这一行数据里，没单独建表。这样查起来简单，一次就把任务和标签都拿到了。

### 5. 缓存策略怎么保证数据一致性？

**为什么用缓存？**
统计数据要算很多，比如"这周完成了多少任务"、"本月专注了多少分钟"，每次都重新算一遍很慢。所以把算好的结果暂存在缓存里，下次直接拿。

**5 分钟缓存怎么运作？**
```
用户打开统计页面
    ↓
问缓存："有没有？"
    ├── 有 → 直接返回，很快（毫秒级）
    └── 没有 → 去数据库算一遍
                → 结果存到缓存，设置 5 分钟后自动过期
                → 返回给用户
```

**怎么保证数据不是太旧的？**
用户做"写操作"时主动清除缓存：
```
用户新建了一个任务
    ↓
删掉对应的缓存
    ↓
下次看统计时 → 缓存没了 → 重新从数据库算 → 拿到最新数据
```

**打个比方：**
就像食堂阿姨做的红烧肉，做一锅能吃 5 分钟（缓存 5 分钟）。有新菜出锅（用户新建任务），阿姨就把上一锅没卖完的倒掉（清理缓存），让大家吃新鲜的。

### 6. 番茄钟有什么实现细节？

**番茄钟是什么？**
一种时间管理方法：专注工作 25 分钟，休息 5 分钟，循环进行。

**前端计时怎么做的？**
- 用定时器每秒钟把剩余时间减 1
- 用一个"变量盒子"保存当前剩余时间
- 页面显示一个圆环进度条，随着时间流逝逐渐闭合

**怎么和后端配合？**
- 专注开始：调接口告诉后端"我开始专注了"，后端存记录
- 专注完成：调接口告诉后端"我完成了"，后端记录实际时长
- 专注中断：同样调接口，但标记为"被打断"

**一个容易出 Bug 的地方：**
剩余时间为 0 时，定时器自动触发完成 + 用户手点"完成"可能同时发生。不加防护会发两次请求。我们的解决方法：加一个"正在完成中"的标记，一旦开始处理就锁住，阻止第二次触发。

**如果用户刷新页面？**
正在进行的番茄钟会丢失（存在内存里，一刷新就没了），但已完成的记录不会丢（已存到数据库）。

### 7. 主题系统怎么实现的？

**效果：** 亮色/暗色切换 + 10 种主题色（拂晓蓝、极客蓝、酱紫、洋红、赤红、日暮橙、金盏黄、极光绿、青碧、深湖蓝）

**做法：** 把用户的选择（亮/暗、什么颜色）存到浏览器的本地小仓库里，每次加载页面时读取。

**小缺陷：** 切换颜色后需要刷新才能全局生效，后续可以优化成即时切换。

### 8. Docker 部署是怎么做的？

**Docker 是什么？**
就像打包工具，把你的应用和它需要的环境打包成一个"集装箱"。放到任何有 Docker 的机器上都能直接运行。

**项目用了几个"集装箱"？**
一共 4 个：MySQL（数据库）、Redis（缓存）、后端程序、前端页面（由 Nginx 托管）。

**启动顺序：**
数据库和缓存先启动 → 后端等它们就绪后再启动 → 前端最后启动。因为后端依赖于数据库和缓存，顺序乱了会报错。

**前端怎么调用后端？**
Nginx 配置了规则：请求地址以 `/api/` 开头的转发给后端。前端只要知道 Nginx 这一个地址就行。

**Docker Compose 的作用：**
一个配置文件，定义了 4 个集装箱的配置和关系。一条命令 `docker-compose up` 所有服务一起启动。

### 9. 前后端怎么通信的？

前端（用户看到的页面）和后端（处理数据的程序）通过网络请求通信。流程：

1. 用户操作（比如点"新建任务"）
2. 前端把数据打包成一个请求
3. 请求带 Token，用 POST 方法发出
4. 后端接收处理、跟数据库交互
5. 返回统一格式的结果（状态码 + 提示信息 + 实际数据）
6. 前端更新页面

所有接口返回格式统一，前端处理起来很方便。

### 10. 如果发现项目有问题，你会怎么改？

四个改进点：

1. **代码重复**：多个地方都写了相同的代码来获取当前登录用户 ID，应该提取公用
2. **分页方式**：现在一次性加载所有任务，数据多了会慢。应改为每次只看一页
3. **主题切换要刷新**：应做到即时生效
4. **数据库密码明文**：配置里直接写 `123456` 不安全，应通过环境变量配置

### 11. 怎么防止一个用户看到另一个用户的任务？

所有操作后端都会校验"这个任务是不是这个用户的"：
- 查询时自动加上"只查属于这个用户"的条件
- 修改/删除时先解析 Token 确定身份，只操作属于该用户的数据
- 批量操作时先全部确认归属
- 防注入：用户输入和命令分开处理，输入不会变成命令被执行

### 12. 项目从零到完成用了多久？

总共约 2-3 周：
- 第 1-3 天：搭后端架子（注册登录、任务增删改查、建表）
- 第 4-6 天：前端基础（登录页、任务列表页、前后端对接）
- 第 7-9 天：核心功能（番茄钟、AI 拆任务、统计图表）
- 第 10-12 天：收尾美化（个人中心、主题色、Docker 部署）

每阶段留了 1 天缓冲。

---

## 面试小贴士

**最可能被问的三个问题：**
1. 双 Token（最核心技术点，概率 90%）
2. AI 降级方案（容错意识，概率 80%）
3. 数据库索引设计（基础功，概率 70%）

**回答技巧：**
- 先说自己做了什么，再说为什么这样做
- 可以适当承认不足："这一点后续可以优化"
- 如果被问到不会的，可以说"我的理解是……"，表示愿意学习

**自我介绍（1-2 分钟）：**
"我做了一个 AI Todo 智能待办清单，全栈项目。核心功能是任务管理加上 AI 帮你拆解任务，还有一个番茄钟帮你保持专注。项目通过 Docker 一键部署。里面我重点处理了两个问题，一个是登录验证的双 Token 机制，一个是 AI 服务不可用时的降级方案。"
