# Campus Forum 校园技术论坛 — 面试准备文档

---

## 项目介绍（一段话）

Campus Forum 是一个校园技术交流社区，就像一个面向大学生的"知乎 + 技术博客"平台。我独立完成了这个项目的全部开发，包括后端（藏在后台处理数据的程序）、前端（你在浏览器里看到的页面）和部署上线。项目设计了 11 张数据库表、44 个接口，核心功能包括文章发布互动（发文章、评论、点赞、收藏、关注）、AI 智能助手（AI 对话、生成图片、AI 写作助手）、用户社交（个人主页、消息通知）、以及后台管理系统（用户管理、文章审核、数据统计）。技术亮点包括自定义"登录检查标记"一键拦截未登录访问、缓存实现浏览量防刷、以及 AI 功能的优雅降级（即使 AI 服务关了，网站其他功能完全不受影响）。

---

## 面试 Q&A

### 1. 自定义"登录检查标记"是怎么实现的？

**问题：**
有些操作需要登录才能做（发布文章、评论），有些不需要（浏览首页）。最直接的方法是在每个接口里判断"用户有没有登录"，但十几个接口就要写十几遍，很重复。

**做法：用"标记 + 拦截器"，一劳永逸**

第一步：自定义一个标记，叫 `@RequireLogin`，就是一个"贴纸"。
第二步：在需要登录的接口上，贴一行 `@RequireLogin` 就行。
第三步：写一个拦截器自动检测——检测到贴纸，就检查有没有登录，没有就报错"请先登录"，有就正常执行。

**用日常例子理解：**
就像进小区刷门禁卡。`@RequireLogin` 就是"贴了封条的门"，保安会拦住检查你的卡；没贴封条的门随便进。新加接口时贴不贴封条就行，不用跟每个保安说一遍。

**还有 @RequireRole：**
不仅检查登录，还检查角色级别。`@RequireRole(1)` 管理员才能访问，`@RequireRole(2)` 超级管理员才能访问。

**当前用户信息存在哪？**
每次请求来了，先解析 Token，把用户 ID 和角色存到 `UserContext` 里。它用了 ThreadLocal——可以理解为"每个请求有一个专属的小便签"，请求处理完就撕掉，互不干扰。

### 2. 缓存怎么实现浏览量 5 分钟防刷？

**问题：**
每篇文章都有浏览量。最直接的做法是每次访问 +1，但有人狂按 F5 会暴涨，而且每次刷新都写数据库性能差。

**方案：用缓存做 5 分钟"防抖窗口"**

用户查看文章，检查缓存里有没有记录：
- 有这个记录 → 5 分钟内看过了，不计数
- 没有这个记录 → 第一次访问，浏览量 +1，在缓存里记录，5 分钟后自动过期

**用日常例子理解：**
就像便利店计数器。一个人进店 +1，店员记下"这个人来过"，5 分钟内再进出就不计数了。5 分钟后店员忘了，重新计数。

**好处：** 防止刷浏览量、减少数据库压力、缓存查询毫秒级不影响体验。

**一个细节：** 5 分钟后刷新还是会 +1 的，但文章浏览量的统计场景这种精度足够了。

### 3. 缓存还用来做什么了？

**Token 黑名单：** 退出登录时把 Token 存到黑名单，有效期跟 Token 剩余时间一致，之后这个 Token 再来就被拦截。

**系统配置缓存：** 网站配置（网站名称、是否开放注册等）查一次就缓存起来，修改时更新。

### 4. AI 功能是怎么实现的？

接入了 DeepSeek 的 AI 能力（外部 AI 服务），提供三个功能：

**① AI 对话：** 聊天机器人，用户发消息，后端发给 AI，拿回复返回。跟 ChatGPT 一样。

**② AI 图片生成：** 用户输入"一只在太空里游泳的猫"，后端发给 AI，返回一张图片。

**③ AI 写作助手：** 用户输入"帮我写一篇关于 Spring Boot 的文章"，后端构造详细的提示词发给 AI，AI 返回一篇完整的 Markdown 文章，用户直接复制发布。

**前端展示：** 三个功能页（对话、写作、生图），漂亮的聊天界面，跟主流 AI 产品差不多。

### 5. AI 优雅降级怎么理解？

**问题：**
AI 是外部服务，一定会出问题（网络波动、超时、额度用完）。但 AI 挂了不能影响网站主体功能。

**方案：加一个"主开关"**

只有配置文件里打开 AI 开关时，才创建 AI 相关的零件。开关关了：
- AI 功能提示"不可用"
- 文章、评论、点赞、登录注册全部正常，完全不受影响

**用日常例子理解：**
就像餐厅的"智能点餐机器人"。机器人坏了，不影响厨师做菜、服务员上菜——只是不能跟机器人聊天了而已。这个开关决定"今天要不要把机器人推出来"。

### 6. 前后端分离架构怎么设计的？

**前后端分离：** 前端（页面）和后端（数据处理程序）是完全分开的两个项目，各自部署，通过接口通信。

前端发请求到后端接口，数据格式统一，每个请求带 Token 验证身份。后端有一个"总处理器"，统一处理各种错误（没登录、没权限、文章不存在、参数错误等），前端不管哪个接口报错都是一个格式。

### 7. 11 张数据库表是怎么设计的？

| 表 | 说明 |
|-----|------|
| user | 用户表（用户名、密码、角色、头像等） |
| article | 文章表（标题、内容、浏览量、点赞数等） |
| comment | 评论表（关联文章、评论内容、支持回复） |
| category | 分类表（文章分类，如 Java、前端、AI） |
| tag | 标签表（如 Spring Boot、React） |
| article_tag | 文章-标签关系表（多对多） |
| user_like | 点赞表（谁赞了哪篇文章） |
| user_collect | 收藏表（谁收藏了哪篇文章） |
| follow | 关注表（用户间关注关系） |
| notification | 通知表（评论、点赞、系统通知等） |
| system_config | 系统配置表（后台动态配置） |

**设计思路：** 文章和标签是多对多关系（一篇文章可以有多个标签，一个标签可以有多篇文章），所以用了中间表。点赞和收藏需要单独记录时间，所以单独建表。通知表用类型字段区分不同通知。

### 8. Docker 部署是怎么做的？

跟 AI Todo 类似，用了 Docker Compose 编排 4 个服务：后端程序、前端页面（Nginx 托管）、数据库、缓存。

**Nginx 两个功能：** ① 前端路由支持（单页应用，找不到路径就返回首页让前端自己处理）；② API 转发（/api/ 开头的请求转发给后端）。

**启动顺序：** 数据库和缓存先启动 → 后端等它们就绪再启动 → 前端最后启动。

### 9. 不同角色权限怎么实现的？

三种角色：**普通用户**（浏览/发文章/评论/点赞）、**管理员**（+管理文章/分类标签）、**超级管理员**（+修改角色/系统配置）。

用 `@RequireRole` 标记方法，自动检查角色级别。`@RequireRole(1)` 管理员以上，`@RequireRole(2)` 只有超级管理员。

**日常例子：** 普通用户=小区业主，管理员=物业经理，超级管理员=开发商。

### 10. 消息通知怎么设计的？

评论、回复、点赞、关注时生成通知。发通知是"异步"的——不影响用户操作速度，通知后台慢慢发。前端右上角小铃铛显示未读数，点开跳转文章，有"全部标记已读"按钮。

### 11. 项目中遇到过什么难点？

**难点一：登录状态全局管理**
每个请求都要知道"当前是谁"，每个接口解析 Token 太冗余。解决方案是三层结构：过滤器拦截请求解析 Token → 上下文持有器存用户信息 → 拦截器检查登录标记。分工明确。

**难点二：AI 不能影响主体功能**
AI 一定会出问题，如果导致全站崩溃就因小失大了。方案是加一个"主开关"，关掉 AI 网站照常运行。

### 12. 这个项目跟 AI Todo 有什么区别？

| 对比 | AI Todo | Campus Forum |
|------|---------|-------------|
| 定位 | 个人效率工具 | 社区社交平台 |
| 核心功能 | 任务管理、番茄钟、统计 | 文章发布、社交互动、AI 助手 |
| 鉴权方式 | 用 Spring Security 框架 | 自定义标记 + 拦截器 |
| 数据库表 | 3 张表 | 11 张表 |
| AI 接入 | 直接调 AI 接口 | 用 Java 的 AI 工具箱调 AI |
| 前端界面 | Ant Design 组件库 | TailwindCSS 样式框架 |
| 特色功能 | 动态主题色、番茄钟 | 后台管理、消息通知、关注 |

**说人话：** AI Todo 是你自己的任务小工具，偏实用；Campus Forum 是社区平台，偏社交。两个项目展示了不同方向的能力。

### 13. GitHub 第三方登录怎么实现的？

让用户用 GitHub 账号直接登录，不用重新注册。

**流程：**
用户点"使用 GitHub 登录" → 跳转 GitHub 授权页 → 用户同意 → GitHub 跳回我们网站带上临时 code → 前端把 code 发后端 → 后端拿着 code 去 GitHub 换 access_token → 获取用户信息（用户名、头像等）→ 查本地有没有这个用户，有就更新，没有就创建 → 返回 Token，登录完成。

**代码结构：** 后端提供一个接口返回 GitHub App ID、一个接口处理登录回调。前端登录页加了个"使用 GitHub 登录"按钮，回调页面自动处理 code 完成登录。

**安全性：** GitHub 用户密码设为随机，没法密码登录，只能走 GitHub。

---

## 面试小贴士

**最可能被问的三个问题：**
1. 登录标记怎么实现的（90%——最大技术亮点）
2. 浏览量怎么防刷（80%——考察设计思维）
3. AI 怎么降级（70%——考察容错意识）

**重点展现的能力：**
- 封装意识：把检查登录做成标记，一次编写到处使用
- 防御思维：考虑到 AI 会挂，提前做了降级
- 性能思维：用缓存防刷，减少数据库压力

**回答技巧：** 先解释痛点再说方案。被问到改进点可以说："用户间私信功能还没做，后续可以加上"。
